---
phase: 04.2-source-cleanup-reexport
plan: 02
type: execute
wave: 2
depends_on: ["04.2-01"]
files_modified: []
autonomous: false
requirements:
  - PLUGIN-03
must_haves:
  truths:
    - "translations.json is regenerated with clean sources (no DC/FC prefixes in entries)"
    - "TextAsset 286 files are regenerated with clean Korean injections"
    - "Plugin.dll is rebuilt with 3-stage TryTranslate"
    - "Patch is deployed to game directory"
    - "In-game: DC/FC choice text displays Korean translation correctly"
    - "translations_loaded >= 75,200 (no regression beyond body-only removal)"
    - "hits_dcfc_strip counter is absent or 0 in state file"
  artifacts:
    - path: "E:/SteamLibrary/steamapps/common/Esoteric Ebb/Esoteric Ebb_Data/StreamingAssets/TranslationPatch/translations.json"
      provides: "Clean translations sidecar without DC/FC body-only entries"
    - path: "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/translation_loader_state.json"
      provides: "Verification metrics: translations_loaded, hits_exact, no dcfc_strip"
  key_links:
    - from: "go-v2-export"
      to: "translations.json"
      via: "BuildV3Sidecar (now DC/FC-free)"
      pattern: "entries.*source"
    - from: "Plugin.dll"
      to: "translation_loader_state.json"
      via: "3-stage TryTranslate writes state on quit"
      pattern: "translations_loaded"
---

<objective>
Regenerate all patch artifacts from the clean DB, build Plugin.dll, deploy to game, and verify in-game that DC/FC choice text still translates correctly with the simplified pipeline.

Purpose: Confirm PLUGIN-03 (95%+ coverage) is maintained after removing DC/FC workarounds. The parser now produces clean sources, so direct matching should cover what DC/FC strip stage previously handled.
Output: Deployed patch with verified in-game functionality, translations_loaded metrics confirming no regression.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.2-source-cleanup-reexport/04.2-CONTEXT.md
@.planning/phases/04.2-source-cleanup-reexport/04.2-01-SUMMARY.md

@workflow/cmd/go-v2-export/main.go
@projects/esoteric-ebb/project.json

<interfaces>
<!-- Rebuild commands from existing CLIs -->

Export CLI:
```bash
go run ./workflow/cmd/go-v2-export --project projects/esoteric-ebb/project.json
```
Produces: translations.json, TextAsset files (286), localizationtexts.csv

Apply-out CLI:
```bash
go run ./projects/esoteric-ebb/cmd/go-esoteric-apply-out --project projects/esoteric-ebb/project.json
```
Produces: ink JSON TextAssets with Korean injected

Patch build:
```powershell
./projects/esoteric-ebb/cmd/build_patch_package_unified.ps1
```
Deploys: Plugin.dll + translations.json + TextAssets + fonts to game directory

Plugin.dll build (dotnet):
```bash
dotnet build projects/esoteric-ebb/patch/mod-loader/EsotericEbb.TranslationLoader/EsotericEbb.TranslationLoader.csproj -c Release
```

State file location:
```
E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\translation_loader_state.json
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate all patch artifacts and deploy</name>
  <files></files>
  <read_first>
    workflow/cmd/go-v2-export/main.go,
    projects/esoteric-ebb/project.json,
    .planning/phases/04.2-source-cleanup-reexport/04.2-01-SUMMARY.md
  </read_first>
  <action>
    **Step 1: Regenerate translations.json + TextAssets + CSV (per D-06):**
    ```bash
    go run ./workflow/cmd/go-v2-export --project projects/esoteric-ebb/project.json
    ```
    This calls BuildV3Sidecar (now without DC/FC body-only entries) and writes all artifacts.

    **Step 2: Regenerate ink JSON TextAssets with Korean:**
    ```bash
    go run ./projects/esoteric-ebb/cmd/go-esoteric-apply-out --project projects/esoteric-ebb/project.json
    ```

    **Step 3: Sanity check translations.json:**
    - Count entries: `jq '.entries | length' <translations.json path>` -- expect ~75,200+ (was 75,789, minus ~579 body-only entries, but some may consolidate)
    - Verify no DC/FC prefixes in sources: `jq '[.entries[] | select(.source | test("^[A-Z]{2}[0-9]+\\s+\\w+-"))] | length' <path>` -- expect 0
    - Verify no DC/FC prefixes in targets: `jq '[.entries[] | select(.target | test("^[A-Z]{2}[0-9]+\\s+\\w+-"))] | length' <path>` -- expect 0

    **Step 4: Build Plugin.dll:**
    ```bash
    dotnet build projects/esoteric-ebb/patch/mod-loader/EsotericEbb.TranslationLoader/EsotericEbb.TranslationLoader.csproj -c Release
    ```

    **Step 5: Deploy patch to game directory:**
    Run the unified patch build script:
    ```powershell
    ./projects/esoteric-ebb/cmd/build_patch_package_unified.ps1
    ```
    Or manually copy:
    - Plugin.dll to `E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\plugins\`
    - translations.json to `E:\SteamLibrary\steamapps\common\Esoteric Ebb\Esoteric Ebb_Data\StreamingAssets\TranslationPatch\`
    - TextAsset files to the same TranslationPatch directory
    - runtime_lexicon.json to TranslationPatch

    **Step 6: Delete old state file** to get fresh metrics:
    ```bash
    rm "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/translation_loader_state.json"
    ```
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && jq ".entries | length" $(jq -r .translation.translations_json_path projects/esoteric-ebb/project.json 2>/dev/null || echo "workflow/output/esoteric-ebb/translations.json")</automated>
  </verify>
  <acceptance_criteria>
    - translations.json exists with entries count >= 75,000
    - Zero entries in translations.json where source matches `^[A-Z]{2}\d+\s+\w+-`
    - Plugin.dll builds successfully with dotnet
    - Patch files deployed to E:\SteamLibrary game directory
    - Old translation_loader_state.json deleted for fresh metrics
  </acceptance_criteria>
  <done>All patch artifacts regenerated from clean DB, Plugin.dll rebuilt with 3-stage chain, patch deployed to game directory.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-game verification -- DC/FC choices and coverage metrics</name>
  <files></files>
  <what-built>
    Complete pipeline cleanup: parser strips DC/FC prefixes at source, export no longer generates workaround entries, Plugin.cs uses 3-stage matching. All artifacts regenerated and deployed.
  </what-built>
  <action>
    Human verification checkpoint (per D-10). User launches the game, navigates to DC/FC stat-check choice scenes, confirms Korean display, then quits and reads translation_loader_state.json metrics. No automated action -- this is a human-verify gate.
  </action>
  <how-to-verify>
    1. Launch Esoteric Ebb from Steam
    2. Navigate to a scene with DC/FC stat-check choices (e.g., dialogue with Diplomacy/Force checks -- look for choices that say things like "[Strength] Push the door" or "[Intelligence] Decipher the rune")
    3. Verify DC/FC choice text displays in Korean (no English fallback, no "DC12 str-" prefix visible)
    4. Play through 2-3 different DC/FC check scenes to confirm consistency
    5. Quit the game (triggers state flush)
    6. Check `E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\translation_loader_state.json`:
       - `translations_loaded` >= 75,200 (no significant regression from 75,789)
       - `hits_dcfc_strip` should be absent (field removed from Plugin.cs)
       - `hits_exact` should be higher than before (absorbing former dcfc_strip hits)
       - `untranslated` count <= 3,130 (no regression)
    7. Report the exact metric values from translation_loader_state.json

    Per D-10: PLUGIN-03 requires 95%+ direct matching coverage. The key metric is:
    coverage = (hits_exact + hits_contextual + hits_lexicon) / (hits_exact + hits_contextual + hits_lexicon + misses)
  </how-to-verify>
  <read_first>
    E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/translation_loader_state.json
  </read_first>
  <verify>
    <automated>cat "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/translation_loader_state.json" 2>/dev/null | jq '{translations_loaded, hits_exact, hits_dcfc_strip, untranslated}'</automated>
  </verify>
  <acceptance_criteria>
    - translation_loader_state.json contains translations_loaded >= 75,200
    - translation_loader_state.json does NOT contain "hits_dcfc_strip" key
    - DC/FC choice text displays in Korean in-game (no English, no prefix garbage)
    - untranslated count <= 3,130
  </acceptance_criteria>
  <done>In-game verification passed: DC/FC choices display Korean, translations_loaded shows no regression, PLUGIN-03 95%+ coverage confirmed.</done>
  <resume-signal>Type "approved" with metrics, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- translations.json entries >= 75,000, zero DC/FC prefixes in source or target
- Plugin.dll builds and deploys successfully
- In-game: DC/FC choices render Korean, no prefix leakage
- translation_loader_state.json: translations_loaded >= 75,200, no hits_dcfc_strip field
- PLUGIN-03: coverage >= 95%
</verification>

<success_criteria>
1. All patch artifacts regenerated from clean DB sources
2. Plugin.dll rebuilt with 3-stage TryTranslate
3. In-game DC/FC choices display Korean correctly
4. translations_loaded shows no significant regression
5. PLUGIN-03 coverage 95%+ confirmed by metrics
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-source-cleanup-reexport/04.2-02-SUMMARY.md`
</output>
