---
phase: 04-plugin-optimize-verify
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: []
autonomous: false
requirements: [PLUGIN-01, PLUGIN-03, VERIFY-02]

must_haves:
  truths:
    - "translations.json contains both entries[] and contextual_entries[] with real pipeline data"
    - "Plugin.cs loads translations.json and textasset .json files without errors"
    - "Game displays Korean text without bold leaks or color leaks"
    - "Hit rate (hits / (hits + misses)) is 99%+ from translation_loader_state.json"
  artifacts: []
  key_links:
    - from: "translations.json"
      to: "Plugin.cs TranslationMap + ContextualMap"
      via: "LoadEntriesFromJson parses entries[] and contextual_entries[]"
      pattern: "translations_loaded.*contextual"
    - from: "textassets/*.json"
      to: "Plugin.cs TextAssetOverrides"
      via: "LoadTextAssetOverrides with *.json pattern"
      pattern: "text_asset_override"
---

<objective>
Deploy v2 patch with updated translations.json (contextual_entries) and simplified Plugin.cs, then verify in-game that Korean text renders correctly with 99%+ hit rate and no tag leaks.

Purpose: This is the final integration verification -- all prior work (parser, translation engine, patch export, plugin optimization) comes together in the actual game. Per D-03, use ENABLE_FULL_CAPTURE mode to collect data, analyze hit/miss rates, and fix any discovered issues.

Output: Verified working v2 Korean patch in game
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-plugin-optimize-verify/04-CONTEXT.md
@.planning/phases/04-plugin-optimize-verify/04-RESEARCH.md
@.planning/phases/04-plugin-optimize-verify/04-01-SUMMARY.md
@.planning/phases/04-plugin-optimize-verify/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate translations.json with contextual_entries</name>
  <files></files>
  <read_first>
    - workflow/cmd/go-v2-export/main.go (export CLI usage and flags)
    - workflow/internal/v2pipeline/export.go (updated BuildV3Sidecar with ContextualEntries)
    - .planning/phases/03-patch-output-full-run/03-03-SUMMARY.md (export CLI usage from Phase 3)
  </read_first>
  <action>
    Run the existing go-v2-export CLI to regenerate translations.json with the updated BuildV3Sidecar that now includes contextual_entries[]:

    ```bash
    cd C:/Users/DELL/Desktop/localize-agent
    go run ./workflow/cmd/go-v2-export -project projects/esoteric-ebb/project.json -min-coverage 0
    ```

    After generation, verify the output file contains both `entries` and `contextual_entries` keys:
    ```bash
    # Check translations.json has contextual_entries
    python -c "import json; d=json.load(open('projects/esoteric-ebb/output/v2/translations.json')); print(f'entries: {len(d[\"entries\"])}, contextual: {len(d[\"contextual_entries\"])}')"
    ```

    Expected: entries count < contextual_entries count (entries is deduped, contextual has all ~35K items).
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('C:/Users/DELL/Desktop/localize-agent/projects/esoteric-ebb/output/v2/translations.json')); assert 'contextual_entries' in d, 'missing contextual_entries'; assert len(d['contextual_entries']) > len(d['entries']), f'contextual({len(d[\"contextual_entries\"])}) should > entries({len(d[\"entries\"])})';"</automated>
  </verify>
  <acceptance_criteria>
    - translations.json file exists at projects/esoteric-ebb/output/v2/translations.json
    - JSON contains "contextual_entries" key
    - len(contextual_entries) > len(entries) (dedup reduces entries count)
    - JSON format field is "esoteric-ebb-sidecar.v3"
  </acceptance_criteria>
  <done>translations.json regenerated with both entries[] (deduped) and contextual_entries[] (all items)</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Deploy v2 patch and run game verification</name>
  <files></files>
  <action>
    User deploys v2 patch and plays through sample scenes with ENABLE_FULL_CAPTURE enabled.

    Deployment steps:
    1. Copy updated translations.json to patch directory:
       `E:\SteamLibrary\steamapps\common\Esoteric Ebb\Esoteric Ebb_Data\StreamingAssets\TranslationPatch\`
    2. Copy textasset .json files to:
       `E:\SteamLibrary\steamapps\common\Esoteric Ebb\Esoteric Ebb_Data\StreamingAssets\TranslationPatch\textassets\`
    3. Build and deploy Plugin.dll (or copy modified Plugin.cs and rebuild)
    4. Create ENABLE_FULL_CAPTURE marker file in:
       `E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\`
    5. Launch game and play through sample scenes:
       - Intro sequence (dialogue, narration)
       - Character creation (UI labels, stats)
       - Dialogue with branches (ink choices)
       - Ability check scene (DC checks)
       - Spell/inventory screen
       - Combat (tooltips, status effects)
    6. Quit game
    7. Check capture files in BepInEx/:
       - translation_loader_state.json (hit/miss counters)
       - full_text_capture.json (all captured text)
       - untranslated_capture.json (missed strings)

    Verification criteria:
    - No bold tag leaks (raw `<b>` or `</b>` visible in game text)
    - No color tag leaks (raw `<color=...>` visible in game text)
    - Korean text displays correctly in all tested scenes
    - translation_loader_state.json shows translations_loaded > 0
  </action>
  <verify>Share translation_loader_state.json content and report any visual issues. Type "verified" if clean, or describe problems found.</verify>
  <done>User has played through sample scenes, confirmed no tag leaks, and capture data files exist in BepInEx/</done>
</task>

<task type="auto">
  <name>Task 3: Analyze capture data and calculate hit rate</name>
  <files></files>
  <read_first>
    - E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/translation_loader_state.json (hit/miss counters after game session)
    - E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/full_text_capture.json (all captured text with origins)
    - E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/untranslated_capture.json (missed strings)
  </read_first>
  <action>
    1. Read translation_loader_state.json and extract hit/miss counters
    2. Calculate hit rate: `hits / (hits + misses) * 100`
    3. If untranslated_capture.json exists, categorize misses by origin type (tmp_text, menu_scan, ink_dialogue, ink_choice)
    4. Report:
       - Total translations loaded
       - Hit count, miss count, hit rate percentage
       - Top 10 most frequent missed strings (if any)
       - Whether 99%+ target (per D-03) is achieved

    If hit rate < 99%:
    - Analyze miss patterns to determine if they are:
      a) Strings that need runtime_lexicon entries (per D-04, defer to post-verification)
      b) Translation gaps (missing from pipeline)
      c) Dynamic text that cannot be pre-translated
    - Report findings for user decision on next steps
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/translation_loader_state.json')); hits=d.get('translation_hit_count',0); misses=d.get('translation_miss_count',0); rate=hits/(hits+misses)*100 if (hits+misses)>0 else 0; print(f'Hit rate: {rate:.1f}% ({hits} hits, {misses} misses)'); assert d.get('translations_loaded',0) > 0, 'translations not loaded'"</automated>
  </verify>
  <acceptance_criteria>
    - translation_loader_state.json has translations_loaded > 0
    - Hit rate calculation completed and reported
    - If misses exist, top missed strings are categorized
    - Hit rate percentage is documented
  </acceptance_criteria>
  <done>Hit rate calculated from capture data. If 99%+, PLUGIN-03 and VERIFY-02 satisfied. If below, miss analysis provided with actionable next steps.</done>
</task>

</tasks>

<verification>
- translations.json contains contextual_entries[] with all pipeline items
- Game runs with v2 patch without crashes
- No tag leaks visible in game UI
- Hit rate from translation_loader_state.json meets 99%+ target
</verification>

<success_criteria>
- translations.json regenerated with contextual_entries (PLUGIN-01 complete)
- v2 patch deployed and game verified with no tag leaks (VERIFY-02)
- Hit rate 99%+ confirmed from capture data (PLUGIN-03)
- All 4 phase requirements (PLUGIN-01, PLUGIN-02, PLUGIN-03, VERIFY-02) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/04-plugin-optimize-verify/04-03-SUMMARY.md`
</output>
