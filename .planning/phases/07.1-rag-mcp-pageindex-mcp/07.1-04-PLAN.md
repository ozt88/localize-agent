---
phase: 07.1-rag-mcp-pageindex-mcp
plan: 04
type: execute
wave: 3
depends_on: [07.1-03]
files_modified:
  - projects/esoteric-ebb/context/ab_test_rag.py
  - projects/esoteric-ebb/context/ab_test_rag_results.json
autonomous: false
requirements: [D-20]

must_haves:
  truths:
    - "RAG 컨텍스트 주입 전후 10개 배치 A/B 테스트에서 스코어가 하락하지 않는다 (사용자 검토 필수 — Task 2 checkpoint)"
    - "테스트 결과가 정량적으로 비교 가능한 형태로 출력된다"
  artifacts:
    - path: "projects/esoteric-ebb/context/ab_test_rag.py"
      provides: "RAG A/B 테스트 스크립트 또는 결과 리포트"
  key_links:
    - from: "A/B test"
      to: "v2pipeline with --rag-context flag"
      via: "CLI 실행 (10 batches)"
      pattern: "go-v2-pipeline.*rag-context"
---

<objective>
RAG 컨텍스트 주입 A/B 테스트 -- 10개 배치에 대해 RAG 주입 전/후 번역 스코어 비교

Purpose: D-20 결정에 따라 RAG 컨텍스트 주입이 번역 품질을 저하시키지 않음을 검증. Phase 07과 동일한 10배치 A/B 테스트 방식.
Output: 테스트 결과 리포트 (스코어 비교)
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-rag-mcp-pageindex-mcp/07.1-CONTEXT.md
@.planning/phases/07.1-rag-mcp-pageindex-mcp/07.1-03-SUMMARY.md
@.planning/phases/07-context-enrichment/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: A/B 테스트 실행 -- RAG 컨텍스트 유무 비교</name>
  <files>projects/esoteric-ebb/context/ab_test_rag.py</files>
  <read_first>
    - .planning/phases/07-context-enrichment/07-03-SUMMARY.md (Phase 07 A/B 테스트 방식 참조)
    - workflow/cmd/go-v2-pipeline/main.go (CLI 플래그 확인 -- rag-context, once, worker-role 등)
    - projects/esoteric-ebb/context/ab_test_diff.txt (Phase 07 A/B 결과 포맷 참조, 존재 시)
  </read_first>
  <action>
    Phase 07 A/B 테스트와 동일한 방식으로 RAG 컨텍스트 유무를 비교.

    **테스트 설계:**
    - 10개 배치 선정 (세계관 관련 대사가 포함된 배치 -- rag_batch_context.json에서 hints가 있는 배치 중 무작위 10개)
    - A 조건: RAG 없이 번역 (기존 방식)
    - B 조건: RAG 있이 번역 (--rag-context 플래그)
    - 평가: 동일 score worker로 양쪽 스코어링

    **실행 방법:**
    1. DB에서 테스트 배치 10개를 StatePendingTranslate로 리셋
    2. A 조건 실행: `go run ./workflow/cmd/go-v2-pipeline --once --worker-role=translate --rag-context=""` (빈 경로 = RAG 없음)
    3. A 결과 스냅샷 저장
    4. 동일 배치를 다시 StatePendingTranslate로 리셋
    5. B 조건 실행: `go run ./workflow/cmd/go-v2-pipeline --once --worker-role=translate --rag-context=projects/esoteric-ebb/rag/rag_batch_context.json`
    6. B 결과 스냅샷 저장
    7. 양쪽 score 실행 후 비교

    **결과 출력:**
    ```
    Batch ID | Score (No RAG) | Score (With RAG) | Delta
    ---------|----------------|------------------|------
    batch_01 | 7.2            | 7.5              | +0.3
    ...
    Average  | 7.1            | 7.3              | +0.2
    ```

    성공 기준: 평균 Delta >= 0 (하락하지 않음)

    스크립트 또는 수동 실행 절차를 ab_test_rag.py에 기록.

    **결과 저장:** 테스트 결과를 `projects/esoteric-ebb/context/ab_test_rag_results.json`에 JSON으로 저장. 포맷: `{"batches": [{"batch_id": "...", "score_no_rag": N, "score_with_rag": M, "delta": M-N}, ...], "avg_delta": X}`
  </action>
  <verify>
    <automated>python -c "import os; assert os.path.exists('projects/esoteric-ebb/context/ab_test_rag_results.json'), 'ab_test_rag_results.json not found'; import json; d=json.load(open('projects/esoteric-ebb/context/ab_test_rag_results.json')); assert 'batches' in d or isinstance(d, list), 'Invalid results structure'; print('A/B results file exists and is valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - 10개 배치에 대해 A(no RAG)/B(with RAG) 양쪽 결과 존재
    - 결과 비교표에 batch_id, score_a, score_b, delta 컬럼 존재
    - 평균 delta >= 0 (RAG 주입이 스코어를 저하시키지 않음)
  </acceptance_criteria>
  <done>RAG 컨텍스트 주입 A/B 테스트 완료, 평균 스코어 하락 없음 확인</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: RAG 파이프라인 전체 결과 검증</name>
  <files>projects/esoteric-ebb/context/ab_test_rag.py</files>
  <action>사용자가 A/B 테스트 결과와 RAG 파이프라인 전체 산출물을 검증한다.</action>
  <verify>
    <automated>echo "checkpoint -- human verification required"</automated>
  </verify>
  <done>사용자가 RAG 파이프라인 결과를 승인</done>
  <what-built>RAG 컨텍스트 주입 전체 파이프라인 -- 위키 데이터 전처리, enriched termbank, 배치별 사전 매칭, Go 런타임 통합, A/B 테스트</what-built>
  <how-to-verify>
    1. A/B 테스트 결과표 확인: 평균 delta >= 0 확인
    2. 임의 배치의 프롬프트 출력에서 "[CONTEXT] (세계관 정보)" 블록 확인
    3. enriched_termbank.json에서 wiki/glossary 소스 분포 확인
    4. rag_batch_context.json에서 커버리지 확인 (hints 있는 배치 비율)
  </how-to-verify>
  <resume-signal>"approved" 또는 이슈 설명</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM 출력 | A/B 테스트의 번역 결과 -- 기존 validation 파이프라인으로 검증 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-10 | T (Tampering) | A/B 결과 | accept | 테스트 스크립트가 DB에서 직접 스코어 읽기, 수동 조작 불가 |
</threat_model>

<verification>
1. A/B 테스트 결과 리포트 존재
2. 평균 스코어 delta >= 0
3. 사용자 승인 (checkpoint)
</verification>

<success_criteria>
- RAG 컨텍스트 주입 후 번역 스코어가 하락하지 않음을 10배치 A/B 테스트로 확인
- 사용자가 결과를 검토하고 승인
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-rag-mcp-pageindex-mcp/07.1-04-SUMMARY.md`
</output>
