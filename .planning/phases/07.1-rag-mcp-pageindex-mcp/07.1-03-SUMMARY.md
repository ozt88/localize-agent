---
phase: 07.1-rag-mcp-pageindex-mcp
plan: 03
subsystem: pipeline-rag-integration
tags: [ragcontext, prompt-injection, context-enrichment, D-17, D-18, D-19]
dependency_graph:
  requires: [07.1-02]
  provides: [ragcontext-package, rag-prompt-injection, rag-score-injection]
  affects: [clustertranslate, v2pipeline, scorellm]
tech_stack:
  added: []
  patterns: [per-batch-rag-lookup, token-budget-priority]
key_files:
  created:
    - workflow/internal/ragcontext/types.go
    - workflow/internal/ragcontext/loader.go
    - workflow/internal/ragcontext/matcher.go
    - workflow/internal/ragcontext/loader_test.go
  modified:
    - workflow/internal/clustertranslate/types.go
    - workflow/internal/clustertranslate/prompt.go
    - workflow/internal/clustertranslate/prompt_test.go
    - workflow/internal/v2pipeline/types.go
    - workflow/internal/v2pipeline/worker.go
    - workflow/internal/v2pipeline/run.go
    - workflow/cmd/go-v2-pipeline/main.go
    - workflow/internal/scorellm/prompt.go
decisions:
  - "D-18 trim priority: continuity -> RAG -> glossary -> branch -> voice (voice card highest priority)"
  - "RAG hints injected as [CONTEXT] block with (세계관 정보) label, after NextKO and before --- separator"
  - "Score prompt gets RAG context as header with (세계관 정보 -- 평가 참고용) label"
  - "RAG context path defaults to projects/<name>/rag/rag_batch_context.json, overridable via -rag-context flag"
metrics:
  duration: 9min
  completed: 2026-04-11
  tasks: 2
  files: 12
---

# Phase 07.1 Plan 03: Go ragcontext Package + Pipeline RAG Integration Summary

ragcontext package provides JSON batch context loader + hint formatter; integrated into translate/score workers with D-18 token budget priority and D-19 dual-injection

## Task 1: ragcontext Package + ClusterTask Extension + Prompt Injection (TDD)

**Commit:** 71f1c64

Created `workflow/internal/ragcontext/` package with three files:
- `types.go`: `RAGHint` struct (term, description, category)
- `loader.go`: `BatchContext` type with `LoadBatchContext(path)` and `HintsForBatch(batchID)` — loads Plan 02's `rag_batch_context.json` into memory, returns hints by batch_id
- `matcher.go`: `FormatHints(hints)` — formats hints as "Term: Description | Term2: Description2" (matching lore.go pattern)

Extended `ClusterTask` with `RAGHints string` field. Updated `buildScriptPromptCore` to inject RAG hints into [CONTEXT] block with "(세계관 정보 -- 번역 참고용)" label. Updated `trimContextForBudget` from 3-phase to 5-phase priority per D-18: continuity -> RAG -> glossary -> branch -> voice.

Tests: 6 ragcontext tests + 3 prompt tests (RAG injection, empty RAG, trim priority ordering).

## Task 2: Worker Integration + Score Prompt + CLI Flag

**Commit:** ce1679a

- Added `RAGContextPath string` to `v2pipeline.Config`
- `Run()` loads RAG context at startup with graceful fallback (warning + empty context on error)
- `TranslateWorker`/`translateBatch`: added `ragCtx *ragcontext.BatchContext` parameter, calls `HintsForBatch(batchID)` + `FormatHints()` to populate `ClusterTask.RAGHints`
- `ScoreWorker`/`scoreBatch`: added `ragCtx` parameter, passes formatted RAG hints to score prompt (D-19)
- `BuildBatchScorePrompt`: added `ragContext string` parameter, prepends "(세계관 정보 -- 평가 참고용)" header when non-empty
- CLI: added `-rag-context` flag to `go-v2-pipeline`

All 19 test suites pass (0 failures across entire workflow module).

## Deviations from Plan

None - plan executed exactly as written.

## Verification

1. `go build ./...` -- success (exit 0)
2. `go test ./...` -- all 19 packages pass
3. `RAGHints` field present in `clustertranslate/types.go`
4. "세계관 정보" present in `clustertranslate/prompt.go`
5. `-rag-context` flag present in `go-v2-pipeline/main.go`

## Self-Check: PASSED

- [x] workflow/internal/ragcontext/types.go exists
- [x] workflow/internal/ragcontext/loader.go exists
- [x] workflow/internal/ragcontext/matcher.go exists
- [x] workflow/internal/ragcontext/loader_test.go exists
- [x] Commit 71f1c64 exists
- [x] Commit ce1679a exists
