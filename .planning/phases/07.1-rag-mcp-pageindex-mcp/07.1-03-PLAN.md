---
phase: 07.1-rag-mcp-pageindex-mcp
plan: 03
type: execute
wave: 3
depends_on: [07.1-02]
files_modified:
  - workflow/internal/ragcontext/types.go
  - workflow/internal/ragcontext/loader.go
  - workflow/internal/ragcontext/matcher.go
  - workflow/internal/ragcontext/loader_test.go
  - workflow/internal/clustertranslate/types.go
  - workflow/internal/clustertranslate/prompt.go
  - workflow/internal/clustertranslate/prompt_test.go
  - workflow/internal/v2pipeline/types.go
  - workflow/internal/v2pipeline/worker.go
  - workflow/internal/v2pipeline/run.go
  - workflow/cmd/go-v2-pipeline/main.go
  - workflow/internal/scorellm/prompt.go
autonomous: true
requirements: [D-10, D-11, D-17, D-18, D-19]

must_haves:
  truths:
    - "Go ragcontext 패키지가 rag_batch_context.json을 로드하고 batch_id로 hints를 반환한다"
    - "ClusterTask.RAGHints 필드에 세계관 컨텍스트가 주입되어 [CONTEXT] 섹션에 나타난다"
    - "trimContextForBudget이 D-18 우선순위(continuity -> RAG -> glossary -> branch -> voice)를 따른다"
    - "번역 worker와 평가 worker 양쪽에서 RAG 컨텍스트가 프롬프트에 포함된다"
    - "기존 lore.go 정적 매칭이 ragcontext로 대체된다 (v2pipeline에서)"
  artifacts:
    - path: "workflow/internal/ragcontext/types.go"
      provides: "RAGEntry, RAGHint 타입 정의"
      contains: "type RAGEntry struct"
    - path: "workflow/internal/ragcontext/loader.go"
      provides: "LoadBatchContext JSON 로더"
      exports: ["LoadBatchContext"]
    - path: "workflow/internal/ragcontext/matcher.go"
      provides: "FormatHints 포맷터"
      exports: ["FormatHints"]
    - path: "workflow/internal/clustertranslate/types.go"
      provides: "ClusterTask.RAGHints 필드"
      contains: "RAGHints"
    - path: "workflow/internal/clustertranslate/prompt.go"
      provides: "[CONTEXT] 섹션 RAG 주입 + trimContextForBudget RAG 계층"
      contains: "세계관 정보"
  key_links:
    - from: "workflow/internal/v2pipeline/worker.go"
      to: "workflow/internal/ragcontext/loader.go"
      via: "LoadBatchContext at startup, lookup per batch"
      pattern: "ragcontext\\.LoadBatchContext"
    - from: "workflow/internal/clustertranslate/prompt.go"
      to: "ClusterTask.RAGHints"
      via: "[CONTEXT] 블록 삽입"
      pattern: "task\\.RAGHints"
---

<objective>
Go ragcontext 패키지 + 파이프라인 통합 -- 사전 매칭된 RAG 컨텍스트를 번역/평가 프롬프트에 주입

Purpose: Plan 02의 rag_batch_context.json을 Go worker가 로드하여, 각 배치의 세계관 힌트를 [CONTEXT] 섹션에 주입. 기존 lore.go를 대체(D-10)하고, 번역+평가 양쪽에 주입(D-19), 토큰 예산 우선순위 업데이트(D-18)
Output: ragcontext 패키지, ClusterTask 확장, 프롬프트 주입, worker 통합
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07.1-rag-mcp-pageindex-mcp/07.1-CONTEXT.md
@.planning/phases/07.1-rag-mcp-pageindex-mcp/07.1-RESEARCH.md
@.planning/phases/07.1-rag-mcp-pageindex-mcp/07.1-02-SUMMARY.md

@workflow/internal/translation/lore.go
@workflow/internal/clustertranslate/types.go
@workflow/internal/clustertranslate/prompt.go
@workflow/internal/v2pipeline/types.go
@workflow/internal/v2pipeline/worker.go
@workflow/internal/v2pipeline/run.go
@workflow/internal/scorellm/prompt.go
@workflow/cmd/go-v2-pipeline/main.go

<interfaces>
<!-- rag_batch_context.json 구조 (Plan 02 산출물): -->
```json
{
  "knot_Alfoz/gate_001": [
    {"term": "Alfoz", "description": "A small town...", "category": "location"},
    {"term": "Norvik", "description": "Your home city...", "category": "location"}
  ]
}
```

<!-- ClusterTask 현재 구조 (확장 대상): -->
```go
type ClusterTask struct {
    Batch            inkparse.Batch
    PrevGateLines    []string
    GlossaryJSON     string
    NextLines        []string
    PrevKO           []string
    NextKO           []string
    VoiceCards       map[string]string
    ParentChoiceText string
    // RAGHints string  ← 추가 예정
}
```

<!-- trimContextForBudget 현재 우선순위: -->
<!-- Phase 1: continuity (NextLines, NextKO, PrevKO) -->
<!-- Phase 2: branch (ParentChoiceText) -->
<!-- Phase 3: voice (VoiceCards) -->
<!-- D-18 업데이트: continuity -> RAG -> glossary -> branch -> voice -->

<!-- ScoreTask 현재 구조: -->
```go
type ScoreTask struct {
    BlockID     string
    ENSource    string
    KOFormatted string
    HasTags     bool
}
```

<!-- Config 현재 구조 (확장 대상): -->
```go
type Config struct {
    // ... existing fields ...
    VoiceCardsPath string
    VoiceCards     map[string]string
    // RAGContextPath string  ← 추가 예정
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ragcontext 패키지 + ClusterTask 확장 + 프롬프트 주입</name>
  <files>
    workflow/internal/ragcontext/types.go,
    workflow/internal/ragcontext/loader.go,
    workflow/internal/ragcontext/matcher.go,
    workflow/internal/ragcontext/loader_test.go,
    workflow/internal/clustertranslate/types.go,
    workflow/internal/clustertranslate/prompt.go,
    workflow/internal/clustertranslate/prompt_test.go
  </files>
  <read_first>
    - workflow/internal/translation/lore.go (formatLoreHints 포맷 패턴)
    - workflow/internal/clustertranslate/types.go (현재 ClusterTask 구조)
    - workflow/internal/clustertranslate/prompt.go (BuildScriptPrompt, trimContextForBudget 전체)
    - workflow/internal/clustertranslate/prompt_test.go (기존 테스트 패턴)
  </read_first>
  <behavior>
    - Test 1: LoadBatchContext가 JSON 파일을 로드하고 batch_id로 hints를 반환
    - Test 2: LoadBatchContext에 존재하지 않는 batch_id 요청 시 빈 slice 반환
    - Test 3: FormatHints가 hint slice를 "(세계관 정보)\nTerm: Description | Term2: Description2" 포맷으로 변환
    - Test 4: FormatHints에 빈 slice 시 빈 문자열 반환
    - Test 5: BuildScriptPrompt에 RAGHints가 있을 때 [CONTEXT] 블록에 "세계관 정보" 텍스트 포함
    - Test 6: trimContextForBudget에서 RAG가 continuity 다음, glossary 전에 제거됨
  </behavior>
  <action>
    **Step 1: ragcontext 패키지 생성**

    `workflow/internal/ragcontext/types.go`:
    ```go
    package ragcontext

    // RAGHint represents a single world-building context hint for prompt injection.
    type RAGHint struct {
        Term        string `json:"term"`
        Description string `json:"description"`
        Category    string `json:"category"`
    }
    ```

    `workflow/internal/ragcontext/loader.go`:
    ```go
    package ragcontext

    import (
        "encoding/json"
        "os"
    )

    // BatchContext holds pre-built RAG hints indexed by batch_id.
    type BatchContext struct {
        hints map[string][]RAGHint
    }

    // LoadBatchContext loads rag_batch_context.json into memory.
    // Returns nil BatchContext (safe to use) if path is empty.
    func LoadBatchContext(path string) (*BatchContext, error) {
        if path == "" {
            return &BatchContext{hints: map[string][]RAGHint{}}, nil
        }
        raw, err := os.ReadFile(path)
        if err != nil {
            return nil, err
        }
        var m map[string][]RAGHint
        if err := json.Unmarshal(raw, &m); err != nil {
            return nil, err
        }
        return &BatchContext{hints: m}, nil
    }

    // HintsForBatch returns the pre-matched hints for a batch_id.
    // Returns empty slice if batch_id not found.
    func (bc *BatchContext) HintsForBatch(batchID string) []RAGHint {
        if bc == nil || bc.hints == nil {
            return nil
        }
        return bc.hints[batchID]
    }
    ```

    `workflow/internal/ragcontext/matcher.go`:
    ```go
    package ragcontext

    import "strings"

    // FormatHints formats RAG hints into a compact string for prompt injection.
    // Format matches lore.go formatLoreHints pattern: "Term: Description | Term2: Description2"
    func FormatHints(hints []RAGHint) string {
        if len(hints) == 0 {
            return ""
        }
        var sb strings.Builder
        for i, h := range hints {
            if i > 0 {
                sb.WriteString(" | ")
            }
            sb.WriteString(h.Term)
            sb.WriteString(": ")
            sb.WriteString(h.Description)
        }
        return sb.String()
    }
    ```

    **Step 2: ClusterTask 확장** (workflow/internal/clustertranslate/types.go)
    - `RAGHints string` 필드 추가 (formatted string, 기존 GlossaryJSON과 동일 패턴):
    ```go
    type ClusterTask struct {
        // ... existing fields ...
        RAGHints         string            // per-batch world-building RAG context (D-17)
    }
    ```

    **Step 3: BuildScriptPrompt RAG 주입** (prompt.go)
    - [CONTEXT] 블록 안에 RAG hints 주입 (PrevGateLines 뒤, --- 구분자 전):
    ```go
    // RAG world-building context (D-17)
    if task.RAGHints != "" {
        prompt.WriteString("[CONTEXT]\n")
        prompt.WriteString("(세계관 정보 -- 번역 참고용)\n")
        prompt.WriteString(task.RAGHints)
        prompt.WriteString("\n")
    }
    ```
    - 위치: `hasContext` 체크에 `task.RAGHints != ""` 조건 추가
    - PrevKO/NextKO 뒤, `---` 구분자 전에 삽입

    **Step 4: trimContextForBudget D-18 업데이트**
    - 현재: Phase 1(continuity) -> Phase 2(branch) -> Phase 3(voice)
    - D-18: continuity -> RAG -> glossary -> branch -> voice
    - 수정된 순서:
    ```go
    func trimContextForBudget(task ClusterTask, maxTokens int) ClusterTask {
        testPrompt, _ := buildScriptPromptCore(task)
        if estimateTokens(testPrompt) <= maxTokens {
            return task
        }
        // Phase 1: continuity (lowest priority)
        task.NextLines = nil; task.NextKO = nil; task.PrevKO = nil
        testPrompt, _ = buildScriptPromptCore(task)
        if estimateTokens(testPrompt) <= maxTokens { return task }

        // Phase 2: RAG hints (D-18: RAG >= glossary)
        task.RAGHints = ""
        testPrompt, _ = buildScriptPromptCore(task)
        if estimateTokens(testPrompt) <= maxTokens { return task }

        // Phase 3: Glossary (D-18)
        task.GlossaryJSON = ""
        testPrompt, _ = buildScriptPromptCore(task)
        if estimateTokens(testPrompt) <= maxTokens { return task }

        // Phase 4: branch context
        task.ParentChoiceText = ""
        testPrompt, _ = buildScriptPromptCore(task)
        if estimateTokens(testPrompt) <= maxTokens { return task }

        // Phase 5: voice cards (last resort)
        task.VoiceCards = nil
        return task
    }
    ```

    **Step 5: 테스트 작성** (TDD RED -> GREEN)
    - loader_test.go: LoadBatchContext + HintsForBatch 테스트 (temp JSON file)
    - prompt_test.go 확장: RAGHints 주입 + trimContextForBudget RAG 계층 테스트
  </action>
  <verify>
    <automated>cd workflow && go test ./internal/ragcontext/... ./internal/clustertranslate/... -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - workflow/internal/ragcontext/ 디렉토리에 types.go, loader.go, matcher.go 존재
    - types.go에 `type RAGHint struct` 정의
    - loader.go에 `func LoadBatchContext(path string) (*BatchContext, error)` 정의
    - loader.go에 `func (bc *BatchContext) HintsForBatch(batchID string) []RAGHint` 정의
    - matcher.go에 `func FormatHints(hints []RAGHint) string` 정의
    - clustertranslate/types.go에 `RAGHints string` 필드 존재
    - prompt.go에 "세계관 정보" 문자열 포함 (RAG 주입 확인)
    - trimContextForBudget에서 RAGHints="" 제거가 ParentChoiceText="" 전에 발생
    - `go test ./internal/ragcontext/... ./internal/clustertranslate/...` 전체 통과
  </acceptance_criteria>
  <done>ragcontext 패키지가 JSON 로드/포맷 제공, ClusterTask에 RAGHints 필드 추가, BuildScriptPrompt가 [CONTEXT]에 RAG 주입, trimContextForBudget이 D-18 우선순위 반영</done>
</task>

<task type="auto">
  <name>Task 2: Worker 통합 + Score 프롬프트 + CLI 플래그</name>
  <files>
    workflow/internal/v2pipeline/types.go,
    workflow/internal/v2pipeline/worker.go,
    workflow/internal/v2pipeline/run.go,
    workflow/cmd/go-v2-pipeline/main.go,
    workflow/internal/scorellm/prompt.go
  </files>
  <read_first>
    - workflow/internal/v2pipeline/worker.go (translateBatch 전체 — RAG 로딩 통합 위치)
    - workflow/internal/v2pipeline/types.go (Config 구조체)
    - workflow/internal/v2pipeline/run.go (worker 초기화, glossary 로딩 패턴)
    - workflow/cmd/go-v2-pipeline/main.go (CLI 플래그 패턴)
    - workflow/internal/scorellm/prompt.go (BuildBatchScorePrompt — RAG 주입 확장)
  </read_first>
  <action>
    **Step 1: Config 확장** (v2pipeline/types.go)
    ```go
    type Config struct {
        // ... existing fields ...
        RAGContextPath string  // path to rag_batch_context.json (optional)
    }
    ```

    **Step 2: run.go — RAG 컨텍스트 로드 at startup**
    glossary 로딩 패턴과 동일하게, Run() 함수에서 ragcontext.LoadBatchContext 호출:
    ```go
    import "localize-agent/workflow/internal/ragcontext"

    // Load RAG batch context (Phase 07.1).
    ragContextPath := resolveProjectPath(projectDir, "rag/rag_batch_context.json")
    if cfg.RAGContextPath != "" {
        ragContextPath = cfg.RAGContextPath
    }
    ragCtx, err := ragcontext.LoadBatchContext(ragContextPath)
    if err != nil {
        fmt.Fprintf(os.Stderr, "v2pipeline RAG context load warning: %v (continuing without RAG)\n", err)
        ragCtx, _ = ragcontext.LoadBatchContext("")
    }
    ```
    - ragCtx를 TranslateWorker, ScoreWorker에 전달 (함수 시그니처 확장)

    **Step 3: TranslateWorker + translateBatch 확장**
    - TranslateWorker 시그니처에 `ragCtx *ragcontext.BatchContext` 추가
    - translateBatch에서 RAG hints 로드:
    ```go
    // RAG world-building context (D-17, Phase 07.1)
    var ragHints string
    if ragCtx != nil {
        hints := ragCtx.HintsForBatch(batchID)
        ragHints = ragcontext.FormatHints(hints)
    }

    task := clustertranslate.ClusterTask{
        // ... existing fields ...
        RAGHints: ragHints,
    }
    ```

    **Step 4: ScoreWorker + scoreBatch 확장 (D-19)**
    - ScoreWorker 시그니처에 `ragCtx *ragcontext.BatchContext` 추가
    - scoreBatch에서 RAG hints를 score 프롬프트에 포함:
    - scorellm/prompt.go BuildBatchScorePrompt에 ragContext 파라미터 추가:
    ```go
    func BuildBatchScorePrompt(tasks []ScoreTask, ragContext string) (string, []string) {
        var b strings.Builder
        // RAG context header (D-19)
        if ragContext != "" {
            fmt.Fprintf(&b, "(세계관 정보 -- 평가 참고용)\n%s\n\n", ragContext)
        }
        fmt.Fprintf(&b, "Score these %d translations...\n\n", len(tasks), len(tasks))
        // ... rest unchanged ...
    }
    ```

    **Step 5: CLI 플래그** (go-v2-pipeline/main.go)
    ```go
    fs.StringVar(&cfg.RAGContextPath, "rag-context", "", "path to rag_batch_context.json")
    ```

    **Step 6: run.go worker 호출 업데이트**
    모든 TranslateWorker, ScoreWorker 호출에 ragCtx 인자 추가.

    빌드 확인: `cd workflow && go build ./...`
  </action>
  <verify>
    <automated>cd workflow && go build ./... 2>&1 && go test ./internal/v2pipeline/... ./internal/scorellm/... -v -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - v2pipeline/types.go에 `RAGContextPath string` 필드 존재
    - v2pipeline/run.go에 `ragcontext.LoadBatchContext` 호출 존재
    - v2pipeline/worker.go translateBatch에 `ragCtx.HintsForBatch` 호출 존재
    - v2pipeline/worker.go scoreBatch에 RAG hints 전달 존재
    - scorellm/prompt.go BuildBatchScorePrompt에 ragContext 파라미터 존재
    - go-v2-pipeline/main.go에 `-rag-context` 플래그 존재
    - `go build ./...` 성공 (exit code 0)
    - 기존 테스트 전체 통과
  </acceptance_criteria>
  <done>v2pipeline이 rag_batch_context.json을 시작 시 로드하고, translateBatch/scoreBatch 양쪽에서 배치별 RAG 컨텍스트를 프롬프트에 주입, CLI 플래그로 경로 설정 가능</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON 파일 -> Go 런타임 | rag_batch_context.json을 신뢰 — 동일 파이프라인의 사전 빌드 산출물 |
| LLM 프롬프트 확장 | RAG 컨텍스트가 프롬프트에 포함 — 토큰 예산 관리 필수 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07.1-07 | T (Tampering) | rag_batch_context.json | accept | 로컬 파이프라인 산출물, 외부 접근 없음 |
| T-07.1-08 | D (DoS) | 토큰 예산 초과 | mitigate | trimContextForBudget에 RAG 계층 추가, 예산 초과 시 RAG hints 자동 제거 |
| T-07.1-09 | E (Elevation) | LLM prompt injection via RAG | accept | RAG 데이터가 공개 위키에서 생성된 정적 텍스트, 사용자 입력 없음 |
</threat_model>

<verification>
1. `cd workflow && go build ./...` 성공
2. `go test ./internal/ragcontext/... ./internal/clustertranslate/... ./internal/v2pipeline/... ./internal/scorellm/...` 전체 통과
3. `grep -r "RAGHints" workflow/internal/clustertranslate/types.go` 매칭
4. `grep -r "세계관 정보" workflow/internal/clustertranslate/prompt.go` 매칭
5. `grep -r "rag-context" workflow/cmd/go-v2-pipeline/main.go` 매칭
</verification>

<success_criteria>
- ragcontext 패키지가 JSON 로드 + 배치별 조회 + 포맷 제공
- translateBatch, scoreBatch 양쪽에서 RAG 주입 동작
- trimContextForBudget이 D-18 우선순위로 RAG 제거
- 전체 빌드 + 기존 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-rag-mcp-pageindex-mcp/07.1-03-SUMMARY.md`
</output>
