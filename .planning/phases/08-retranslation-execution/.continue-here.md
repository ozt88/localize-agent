---
context: phase
phase: 08-retranslation-execution
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-04-12T04:07:06.590Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: worktree-base-bug — Windows에서 git worktree add가 잘못된 베이스로 브랜치를 생성하여 머지 시 325개 파일이 삭제됨 — **workflow.use_worktrees=false 설정 완료. 절대 true로 되돌리지 말 것.**

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| worktree-wrong-base | Windows에서 git worktree add가 현재 HEAD가 아닌 오래된 커밋을 베이스로 브랜치 생성 → 머지 시 파일 대량 삭제 | blocking | workflow.use_worktrees=false (이미 설정됨). 변경 금지. |

<current_state>
Phase 08 Wave 1 완료, Wave 2 파이프라인 완료 대기 중.

- Plan 01: 완전 완료 (SUMMARY.md 존재, 35,028건 pending_translate 리셋)
- Plan 02 Task 1: diff_translations.py 작성 완료 (commit 94e1b11). 나머지(before 백업 + export + diff)는 파이프라인 완료 후 진행 필요.
- Plan 02 Task 2: 미시작 (인게임 검증 체크포인트)

DB 현황 (세션 종료 시점): done=3100, pending_translate=18398, failed=13470, working_translate=67
→ 파이프라인 실행 중. 완료(done >= 34000, pending=0)될 때까지 export 불가.
</current_state>

<completed_work>

**Plan 01 — 전부 완료:**
- Task 1: BuildV3Sidecar highest-gen 2-pass dedup (export.go), ResetAllForRetranslation store 메서드 (store.go), RetranslationGen 필드 (contracts/v2pipeline.go) — commit 56fa1ea
- Task 2: go-v2-reset-all CLI 작성 + 실제 리셋 실행 (35,028건 → pending_translate, retranslation_gen=1) — commit 524c20d

**Plan 02 부분 완료:**
- diff_translations.py 작성 (projects/esoteric-ebb/tools/diff_translations.py) — commit 94e1b11

**인프라 변경:**
- workflow.use_worktrees = false (worktree 머지 버그 수정)
- permissions.defaultMode = bypassPermissions (C:\Users\DELL\.claude\settings.json)
</completed_work>

<remaining_work>

**Plan 02 Task 1 나머지 (파이프라인 완료 후):**
1. DB 상태 확인: `SELECT state, COUNT(*) FROM pipeline_items_v2 GROUP BY state` → done >= 98%
2. before 백업: `cp "E:/SteamLibrary/.../translations.json" "projects/esoteric-ebb/output/translations_before_gen1.json"`
3. go-v2-export 실행:
   ```
   go run ./workflow/cmd/go-v2-export \
     --backend postgres \
     --dsn "postgres://postgres@127.0.0.1:5433/localize_agent?sslmode=disable" \
     --out-dir "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader" \
     --textasset-dir "projects/esoteric-ebb/extract/1.1.3/ExportedProject/Assets/StreamingAssets" \
     --project-dir "projects/esoteric-ebb/output/batches/canonical_full_retranslate_live"
   ```
4. diff 실행:
   ```
   python projects/esoteric-ebb/tools/diff_translations.py \
     "projects/esoteric-ebb/output/translations_before_gen1.json" \
     "E:/SteamLibrary/.../translations.json" \
     --out "projects/esoteric-ebb/output/diff_gen0_vs_gen1.json"
   ```
5. diff 결과 요약 사용자에게 보고

**Plan 02 Task 2 (체크포인트 — 사용자 직접 수행):**
- 게임 실행 후 초반 씬 한국어 렌더링 확인
- 태그 깨짐 없는지, 대사 흐름 자연스러운지 확인
- "approved" 또는 구체적 문제 피드백 제공
</remaining_work>

<decisions_made>

- workflow.use_worktrees = false: Windows worktree 베이스 버그로 325개 파일 삭제 사고 → 순차 실행으로 전환
- permissions.defaultMode = bypassPermissions: 사용자 요청, 권한 팝업 비활성화 (세션 재시작 후 적용)
- D-02 BuildV3Sidecar 2-pass dedup: first-seen-wins → highest-gen-wins, contextual_entries는 전체 유지
- retranslation_gen 컬럼: ALTER TABLE IF NOT EXISTS로 런타임 동적 추가 (기존 스키마에 없음)
- snapshot_at: TEXT(RFC3339) 사용 (TIMESTAMPTZ 아님) — nowValue()와 타입 불일치 방지
</decisions_made>

<blockers>

- **파이프라인 완료 대기**: 재번역 파이프라인이 실행 중 (세션 종료 시점 9% 완료). 사용자가 완료를 확인 후 resume.
- **bypassPermissions 미적용**: settings.json에 설정했으나 현재 세션에는 미적용 — 세션 재시작 필요.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-retranslation-execution/08-02-PLAN.md` — Wave 2 플랜 상세 (Task 1 액션, export 명령, diff 명령)
2. `.planning/phases/08-retranslation-execution/08-01-SUMMARY.md` — Wave 1 결과 (dedup 구현, 리셋 완료)

## Infrastructure State
- PostgreSQL: 127.0.0.1:5433, DB=localize_agent
- 파이프라인: go-v2-pipeline 실행 중 (재번역 진행 중)
- translations.json 현재 위치: `E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader/translations.json`
- workflow.use_worktrees: false (변경 금지)
- GSD executor model: opus, verifier model: sonnet

<context>
Wave 1에서 코드 작업(dedup + CLI + 리셋)을 완료하고 35,028건을 pending_translate로 리셋했다.
파이프라인이 재번역을 진행 중이며, 완료 후 export → diff → 인게임 검증 순서로 Wave 2를 마무리해야 한다.
bypassPermissions 설정을 위해 세션을 재시작한다.
</context>

<next_action>
파이프라인 완료 확인:
```sql
SELECT state, COUNT(*) FROM pipeline_items_v2 GROUP BY state;
```
done >= 34000 (98%)이면 before 백업 후 go-v2-export 실행.
미완료라면 사용자에게 현황 보고 후 대기.
</next_action>
