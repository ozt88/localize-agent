---
phase: 08-retranslation-execution
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - projects/esoteric-ebb/tools/diff_translations.py
autonomous: false
requirements:
  - RETRANS-04
  - RETRANS-05

must_haves:
  truths:
    - "재번역 완료 후 go-v2-export가 highest-gen dedup으로 translations.json을 생성한다"
    - "before/after diff가 변경된 번역 목록을 보여준다"
    - "게임에서 태그 깨짐 없이 한국어가 렌더링된다"
  artifacts:
    - path: "projects/esoteric-ebb/tools/diff_translations.py"
      provides: "translations.json before/after diff 도구"
      contains: "diff_translations"
    - path: "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader/translations.json"
      provides: "재번역된 패치 파일"
      contains: "esoteric-ebb-sidecar.v3"
  key_links:
    - from: "go-v2-export"
      to: "translations.json"
      via: "BuildV3Sidecar highest-gen dedup"
      pattern: "esoteric-ebb-sidecar.v3"
---

<objective>
파이프라인 실행 완료를 확인하고, export + diff + 게임 검증을 수행한다.

Purpose: D-03(완료 후 1회 export), D-05(before/after diff), D-04(초반 씬 회귀 검증)를 실행하여 재번역 품질을 확인한다.
Output: 신규 translations.json, diff 보고서, 게임 검증 결과.

**전제조건:** Plan 01 완료 후 사용자가 파이프라인을 실행하여 35,009건 재번역이 완료된 상태여야 한다.
파이프라인 실행 명령:
```
go run ./workflow/cmd/go-v2-pipeline --project-dir projects/esoteric-ebb/output/batches/canonical_full_retranslate_live --backend postgres --dsn "postgres://postgres@127.0.0.1:5433/localize_agent?sslmode=disable" --cleanup-stale-claims --voice-cards projects/esoteric-ebb/context/voice_cards.json --rag-context projects/esoteric-ebb/rag/rag_batch_context.json --role all
```
</objective>

<execution_context>
@C:\Users\DELL\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\DELL\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-retranslation-execution/08-CONTEXT.md
@.planning/phases/08-retranslation-execution/08-RESEARCH.md
@.planning/phases/08-retranslation-execution/08-01-SUMMARY.md

<interfaces>
<!-- Export CLI flags -->
From workflow/cmd/go-v2-export/main.go:
```
--backend postgres
--dsn "postgres://postgres@127.0.0.1:5433/localize_agent?sslmode=disable"
--out-dir "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader"
--textasset-dir "projects/esoteric-ebb/extract/1.1.3/ExportedProject/Assets/StreamingAssets"
--project-dir "projects/esoteric-ebb/output/batches/canonical_full_retranslate_live"
```

translations.json 현재 위치:
E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader/translations.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 파이프라인 완료 확인 + before 백업 + export + diff 생성</name>
  <files>
    projects/esoteric-ebb/tools/diff_translations.py
  </files>
  <read_first>
    workflow/cmd/go-v2-export/main.go
    workflow/internal/v2pipeline/export.go
    E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader/translations.json
  </read_first>
  <action>
**파이프라인 완료 확인:**

1. DB 상태 확인 -- `psql` 또는 Go CLI로:
```sql
SELECT state, COUNT(*) FROM pipeline_items_v2 GROUP BY state;
```
- `done` 비율이 98% 이상이어야 진행 (v2-export 자체 fail-rate 20% 기준 abort 로직 존재)
- `pending_translate > 0`이면 파이프라인 미완료 -- 사용자에게 알림

**before 백업 + export (Per D-03):**

2. 현재 translations.json을 diff용으로 복사:
```bash
cp "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader/translations.json" \
   "projects/esoteric-ebb/output/translations_before_gen1.json"
```

3. go-v2-export 실행 (D-03: 1회 실행으로 완전 교체):
```bash
go run ./workflow/cmd/go-v2-export \
  --backend postgres \
  --dsn "postgres://postgres@127.0.0.1:5433/localize_agent?sslmode=disable" \
  --out-dir "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader" \
  --textasset-dir "projects/esoteric-ebb/extract/1.1.3/ExportedProject/Assets/StreamingAssets" \
  --project-dir "projects/esoteric-ebb/output/batches/canonical_full_retranslate_live"
```

**diff 도구 작성 + 실행 (Per D-05):**

4. `projects/esoteric-ebb/tools/diff_translations.py` 작성:
```python
#!/usr/bin/env python3
"""Compare two translations.json files and output changed entries.

Usage:
    python diff_translations.py <before.json> <after.json> [--out diff.json] [--summary]
"""
import json
import sys
import argparse

def diff_translations(before_path, after_path):
    with open(before_path, encoding="utf-8") as f:
        before_entries = json.load(f)["entries"]
    with open(after_path, encoding="utf-8") as f:
        after_entries = json.load(f)["entries"]

    before_map = {e["source"]: e["target"] for e in before_entries}
    after_map = {e["source"]: e["target"] for e in after_entries}

    all_sources = set(before_map) | set(after_map)
    changes = []
    unchanged = 0
    added = 0
    removed = 0

    for src in sorted(all_sources):
        b = before_map.get(src)
        a = after_map.get(src)
        if b == a:
            unchanged += 1
        elif b is None:
            added += 1
            changes.append({"source": src, "before": None, "after": a, "type": "added"})
        elif a is None:
            removed += 1
            changes.append({"source": src, "before": b, "after": None, "type": "removed"})
        else:
            changes.append({"source": src, "before": b, "after": a, "type": "changed"})

    return changes, {"total": len(all_sources), "changed": len([c for c in changes if c["type"]=="changed"]),
                      "added": added, "removed": removed, "unchanged": unchanged}

def main():
    parser = argparse.ArgumentParser(description="Diff translations.json files")
    parser.add_argument("before", help="Before translations.json")
    parser.add_argument("after", help="After translations.json")
    parser.add_argument("--out", default=None, help="Output diff JSON path")
    parser.add_argument("--summary", action="store_true", help="Print summary only")
    args = parser.parse_args()

    changes, stats = diff_translations(args.before, args.after)

    print(f"Total entries: {stats['total']}")
    print(f"Changed: {stats['changed']}")
    print(f"Added: {stats['added']}")
    print(f"Removed: {stats['removed']}")
    print(f"Unchanged: {stats['unchanged']}")

    if args.out:
        with open(args.out, "w", encoding="utf-8") as f:
            json.dump({"stats": stats, "changes": changes}, f, ensure_ascii=False, indent=2)
        print(f"Diff written to {args.out}")

    if not args.summary and not args.out:
        # Print first 20 changes as preview
        for c in changes[:20]:
            print(f"\n[{c['type']}] {c['source'][:60]}")
            if c['before']: print(f"  BEFORE: {c['before'][:80]}")
            if c['after']:  print(f"  AFTER:  {c['after'][:80]}")
        if len(changes) > 20:
            print(f"\n... and {len(changes)-20} more changes")

if __name__ == "__main__":
    main()
```

5. diff 실행:
```bash
python projects/esoteric-ebb/tools/diff_translations.py \
  "projects/esoteric-ebb/output/translations_before_gen1.json" \
  "E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader/translations.json" \
  --out "projects/esoteric-ebb/output/diff_gen0_vs_gen1.json"
```

6. diff 결과 요약을 사용자에게 보고 (변경/추가/제거/유지 수치).
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && python projects/esoteric-ebb/tools/diff_translations.py --help</automated>
  </verify>
  <acceptance_criteria>
    - projects/esoteric-ebb/tools/diff_translations.py 파일 존재
    - projects/esoteric-ebb/output/translations_before_gen1.json 파일 존재 (before 백업)
    - E:/SteamLibrary/steamapps/common/Esoteric Ebb/BepInEx/plugins/TranslationLoader/translations.json 갱신됨
    - projects/esoteric-ebb/output/diff_gen0_vs_gen1.json 파일 존재 (diff 결과)
    - diff JSON에 "stats" 키 존재하고 "changed" 값이 0보다 큼
    - grep "esoteric-ebb-sidecar.v3" 신규 translations.json에서 포맷 확인
  </acceptance_criteria>
  <done>
    export 완료된 translations.json이 게임 폴더에 존재하고, before/after diff 보고서가 생성됨
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 인게임 회귀 검증</name>
  <files>none</files>
  <action>
Per D-04, D-05: 사용자가 인게임 검증을 수행한다. Claude는 diff 결과 요약과 검증 절차를 안내한다.

1. diff 결과 요약 출력: `projects/esoteric-ebb/output/diff_gen0_vs_gen1.json`에서 stats 읽어 변경 수치 보고
2. 검증 대상 안내: 이전에 테스트한 초반 진입 씬 고정 리스트
3. 사용자 응답 대기
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && python -c "import json; d=json.load(open('projects/esoteric-ebb/output/diff_gen0_vs_gen1.json')); print('stats:', d['stats'])"</automated>
  </verify>
  <acceptance_criteria>
    - 사용자가 "approved" 응답 또는 구체적 문제 피드백 제공
    - diff_gen0_vs_gen1.json 파일이 존재하고 stats.changed > 0
  </acceptance_criteria>
  <done>
    사용자가 인게임 검증을 완료하고 결과를 피드백함
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| export -> game files | translations.json 교체 시 기존 패치 덮어쓰기 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure | translations_before backup | accept | 번역 데이터는 공개 게임 텍스트이므로 민감하지 않음 |
| T-08-04 | Tampering | translations.json 교체 | mitigate | before 백업 + git tracked으로 롤백 가능. D-03에 따라 백업 없이 완전 교체하되 git으로 복구 |
</threat_model>

<verification>
1. translations.json이 게임 폴더에 존재하고 format="esoteric-ebb-sidecar.v3"
2. diff_gen0_vs_gen1.json에 stats.changed > 0
3. 사용자가 인게임 검증에서 "approved" 응답
</verification>

<success_criteria>
- 재번역된 translations.json이 게임에 적용됨
- before/after diff 보고서 생성됨
- 게임에서 태그 깨짐 없이 렌더링 확인됨
- 대사 흐름이 자연스러움이 확인됨
</success_criteria>

<output>
After completion, create `.planning/phases/08-retranslation-execution/08-02-SUMMARY.md`
</output>
