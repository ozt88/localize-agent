---
phase: 04.1-plugin-cs-v2-inserted
plan: 02
type: execute
wave: 2
depends_on: [04.1-01]
files_modified:
  - E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\plugins\EsotericEbb.TranslationLoader.dll
autonomous: false
requirements: [PLUGIN-02]

must_haves:
  truths:
    - "Plugin.dll v2 is deployed to BepInEx/plugins/ and loads successfully in-game"
    - "TranslationMap loads 75,000+ entries from V3 sidecar translations.json"
    - "Korean text displays in-game without tag leaks (bold, color)"
    - "Choice text shows correct numbering (no StripQuotationMarks interference)"
  artifacts:
    - path: "E:\\SteamLibrary\\steamapps\\common\\Esoteric Ebb\\BepInEx\\plugins\\EsotericEbb.TranslationLoader.dll"
      provides: "Compiled v2 plugin deployed to game"
    - path: "E:\\SteamLibrary\\steamapps\\common\\Esoteric Ebb\\BepInEx\\translation_loader_state.json"
      provides: "Runtime state showing successful v2 loading"
  key_links:
    - from: "EsotericEbb.TranslationLoader.dll"
      to: "translations.json"
      via: "LoadEntriesFromJson at startup"
      pattern: "translations_loaded"
    - from: "translation_loader_state.json"
      to: "Plugin.cs WriteState"
      via: "JSON counter output"
      pattern: "hits_by_stage"
---

<objective>
Build, deploy, and verify Plugin.cs v2 in-game. Confirm translations load correctly, Korean text displays without tag issues, and capture infrastructure works.

Purpose: Compilation alone does not prove correctness. In-game verification catches integration issues (assembly timing, reflection failures, font loading, patch activation).
Output: Deployed Plugin.dll v2, verified translation_loader_state.json showing successful loading
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-plugin-cs-v2-inserted/04.1-CONTEXT.md
@.planning/phases/04.1-plugin-cs-v2-inserted/04.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Plugin.dll and deploy to BepInEx</name>
  <files>E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\plugins\EsotericEbb.TranslationLoader.dll</files>
  <read_first>
    - projects/esoteric-ebb/patch/mod-loader/EsotericEbb.TranslationLoader/EsotericEbb.TranslationLoader.csproj
    - .planning/phases/04.1-plugin-cs-v2-inserted/04.1-01-SUMMARY.md
  </read_first>
  <action>
1. Build Plugin.dll:
```bash
cd projects/esoteric-ebb/patch/mod-loader/EsotericEbb.TranslationLoader
dotnet build /p:BepInExRoot="E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx" -c Release
```

2. Copy the built DLL to BepInEx plugins directory:
```bash
cp projects/esoteric-ebb/patch/mod-loader/EsotericEbb.TranslationLoader/bin/Release/net6.0/EsotericEbb.TranslationLoader.dll "E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\plugins\"
```

3. Verify the deployed DLL exists and is recent (within last 5 minutes).

4. Delete any stale state files to force clean v2 state generation:
```bash
rm -f "E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\translation_loader_state.json"
rm -f "E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\untranslated_capture.json"
```
  </action>
  <verify>
    <automated>ls -la "E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\plugins\EsotericEbb.TranslationLoader.dll" 2>&1 | head -3</automated>
  </verify>
  <acceptance_criteria>
    - File `E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\plugins\EsotericEbb.TranslationLoader.dll` exists
    - `dotnet build -c Release` completed with 0 errors
    - Stale state files removed (translation_loader_state.json, untranslated_capture.json)
  </acceptance_criteria>
  <done>Plugin.dll v2 built and deployed to BepInEx plugins directory, ready for in-game testing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-game verification of Plugin.cs v2</name>
  <files>E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\translation_loader_state.json</files>
  <action>
Human verifies Plugin.cs v2 in-game. This is a checkpoint — no automated action. The user launches the game, checks Korean text display, and reports results.

What was built: Plugin.cs v2 complete rewrite with V3 sidecar loading, 4-stage TryTranslate chain, 11 Harmony patches, Korean font loading, capture infrastructure. Deployed as EsotericEbb.TranslationLoader.dll to BepInEx/plugins/.

Steps for the user:
1. Launch Esoteric Ebb via Steam
2. Check BepInEx console (alt-tab or check LogOutput.log): look for "v2 loaded:" log line showing entry counts, confirm NO red error lines about format mismatch or patch failures
3. Main Menu: Verify Korean text appears (새 게임, 불러오기, 옵션 etc.)
4. Start or Load a game: Check dialogue text displays in Korean
5. Verify no tag leaks: Look for raw &lt;b&gt;, &lt;/b&gt;, &lt;color=...&gt; visible in dialogue
6. Check choices: Verify numbered choices (1, 2, 3...) show Korean text with correct numbering
7. Exit game and check: translation_loader_state.json should show translations_loaded > 70000 with hits_by_stage counters; untranslated_capture.json should exist with untranslated strings
  </action>
  <verify>
    <automated>cat "E:\SteamLibrary\steamapps\common\Esoteric Ebb\BepInEx\translation_loader_state.json" 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - translation_loader_state.json exists and contains "translations_loaded" with value > 70000
    - translation_loader_state.json contains "hits_by_stage" with non-zero counters
    - User confirms Korean text visible in menus and dialogue
    - User confirms no visible tag leaks (bold, color)
    - User confirms choice numbering preserved
  </acceptance_criteria>
  <done>User has verified Plugin.cs v2 works correctly in-game: Korean text displays, no tag leaks, choices numbered correctly, state file shows successful loading</done>
</task>

</tasks>

<verification>
1. Plugin.dll builds and deploys without errors
2. In-game: Korean text visible in menus, dialogue, choices
3. No tag leaks (bold, color) visible in game
4. translation_loader_state.json shows successful v2 loading with 70K+ entries
5. Choice numbering preserved (1, 2, 3... visible before Korean text)
</verification>

<success_criteria>
- Plugin.dll v2 deploys and loads in-game
- User confirms Korean text displays correctly
- translation_loader_state.json shows hits_by_stage counters active
- No visible tag leaks or numbering issues
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-plugin-cs-v2-inserted/04.1-02-SUMMARY.md`
</output>
