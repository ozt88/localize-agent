---
phase: 06-foundation-cli
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - projects/esoteric-ebb/context/speaker_allow_list.json
  - workflow/internal/inkparse/speaker_allowlist.go
  - workflow/internal/inkparse/speaker_allowlist_test.go
  - workflow/internal/inkparse/parser.go
  - workflow/internal/inkparse/parser_test.go
autonomous: false
requirements:
  - SPEAKER-01
  - SPEAKER-02
  - SPEAKER-03

must_haves:
  truths:
    - "pipeline_items_v2의 speaker 커버리지가 대화 라인 대비 비율로 측정되어 있다"
    - "검증된 화자 allow-list JSON이 존재하고, isSpeakerTag 오인식을 필터링할 수 있다"
    - "allow-list에 포함되지 않은 speaker_hint는 거부된다"
    - "커버리지 90% 미달 시 isSpeakerTag 파서가 강화되어 커버리지가 개선된다"
  artifacts:
    - path: "projects/esoteric-ebb/context/speaker_allow_list.json"
      provides: "검증된 화자 이름 목록"
      contains: "speakers"
    - path: "workflow/internal/inkparse/speaker_allowlist.go"
      provides: "allow-list 로드 + 검증 함수"
      contains: "LoadSpeakerAllowList"
    - path: "workflow/internal/inkparse/speaker_allowlist_test.go"
      provides: "allow-list 로드/필터 테스트"
      contains: "TestLoadSpeakerAllowList"
  key_links:
    - from: "workflow/internal/inkparse/speaker_allowlist.go"
      to: "projects/esoteric-ebb/context/speaker_allow_list.json"
      via: "JSON file load"
      pattern: "speaker_allow_list\\.json"
---

<objective>
pipeline_items_v2의 speaker 커버리지를 감사하고, 빈도 기반 + ink 교차 검증으로 화자 allow-list를 생성하며, allow-list 기반 필터링 함수를 구현한다. 커버리지 90% 미달 시 isSpeakerTag 파서를 강화한다.

Purpose: 화자 데이터 정제로 Phase 07 voice card 주입의 정확도 보장
Output: speaker_allow_list.json + allow-list 로드/필터 Go 코드 + (조건부) 파서 강화
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation-cli/06-CONTEXT.md
@.planning/phases/06-foundation-cli/06-RESEARCH.md

<interfaces>
<!-- Key types the executor needs -->

From workflow/internal/inkparse/parser.go (isSpeakerTag 관련):
```go
func isSpeakerTag(tag string) bool  // 현재 휴리스틱 기반 판별
func isGameCommandTag(tag string) bool  // 하드코딩 명령 태그 목록
```

isSpeakerTag 현재 로직 요약:
1. DC/FC prefix -> false
2. dot prefix (.DrummerIntro==1) -> false
3. ALL CAPS (len>1) -> false
4. known ability scores (wis/str/int/cha/dex/con) + "speaker", "reply" -> true
5. isGameCommandTag -> false (CamelCase, underscore, digits, dice notation 등)
6. 첫 글자 대문자 + 나머지 소문자 -> true (Proper case = character name)
7. all lowercase short word -> true
8. else -> false

From workflow/internal/contracts/v2pipeline.go:
```go
type V2PipelineItem struct {
    Speaker     string  // speaker label from ink # tag
    ContentType string  // dialogue, spell, ui, item, system
    // ...
}
```

SQL queries from RESEARCH.md:
```sql
-- 화자 커버리지 감사
SELECT COUNT(*) AS total_dialogue,
       COUNT(CASE WHEN speaker != '' THEN 1 END) AS with_speaker,
       ROUND(100.0 * COUNT(CASE WHEN speaker != '' THEN 1 END) / COUNT(*), 1) AS coverage_pct
FROM pipeline_items_v2
WHERE content_type = 'dialogue';

-- 고유 화자 + 빈도 분포
SELECT speaker, COUNT(*) AS cnt
FROM pipeline_items_v2
WHERE speaker != ''
GROUP BY speaker
ORDER BY cnt DESC;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB 화자 커버리지 감사 + allow-list JSON 생성 + Go 필터 함수</name>
  <files>projects/esoteric-ebb/context/speaker_allow_list.json, workflow/internal/inkparse/speaker_allowlist.go, workflow/internal/inkparse/speaker_allowlist_test.go</files>
  <read_first>
    - workflow/internal/inkparse/parser.go (isSpeakerTag, isGameCommandTag 전체 로직)
    - workflow/internal/v2pipeline/postgres_v2_schema.sql (pipeline_items_v2 스키마)
    - workflow/internal/v2pipeline/store.go (DB 연결 패턴, rebind)
    - workflow/internal/translation/lore.go (JSON 로드 패턴 참조)
    - projects/esoteric-ebb/project.json (checkpoint_dsn 경로 확인)
  </read_first>
  <action>
per D-04, D-05, D-06: 2단계 화자 검증 수행 + allow-list JSON + Go 필터 구현.

**Step 1: DB 감사 (커버리지 + 빈도 분포 추출)**

프로젝트 PostgreSQL에 직접 쿼리하여 화자 데이터 수집. project.json의 `checkpoint_dsn` 값을 사용하여 psql 또는 Go 코드로 아래 쿼리 실행:

```sql
-- 커버리지 비율
SELECT COUNT(*) AS total_dialogue,
       COUNT(CASE WHEN speaker != '' THEN 1 END) AS with_speaker,
       ROUND(100.0 * COUNT(CASE WHEN speaker != '' THEN 1 END) / COUNT(*), 1) AS coverage_pct
FROM pipeline_items_v2
WHERE content_type = 'dialogue';

-- 고유 화자 + 빈도 (오인식 후보 탐지용)
SELECT speaker, COUNT(*) AS cnt
FROM pipeline_items_v2
WHERE speaker != ''
GROUP BY speaker
ORDER BY cnt DESC;
```

결과를 기록하고, 빈도 1-2회인 항목을 오인식 의심 목록으로 분류.

**Step 2: speaker_allow_list.json 생성**

빈도 분포에서 확인된 실제 화자를 allow-list에 포함. 형식:
```json
{
  "version": 1,
  "description": "Verified speaker names from pipeline_items_v2 + ink source cross-check",
  "speakers": [
    {"name": "Braxo", "frequency": 1234, "verified": true},
    {"name": "wis", "frequency": 567, "verified": true}
  ],
  "rejected": [
    {"name": "SuspectTag", "frequency": 2, "reason": "not found in ink source"}
  ]
}
```

파일 경로: `projects/esoteric-ebb/context/speaker_allow_list.json`

빈도 1-2회 항목은 ink JSON 소스 파일(projects/esoteric-ebb/ 아래 TextAsset들)에서 `# SuspectTag` 패턴을 grep하여 교차 검증. ink에서 확인되면 allow, 미확인이면 rejected에 기록.

**Step 3: Go allow-list 로드 + 필터 함수**

`workflow/internal/inkparse/speaker_allowlist.go` 신규 파일 생성:

```go
package inkparse

import (
    "encoding/json"
    "fmt"
    "os"
    "strings"
)

type SpeakerEntry struct {
    Name      string `json:"name"`
    Frequency int    `json:"frequency"`
    Verified  bool   `json:"verified"`
}

type SpeakerAllowList struct {
    Version     int            `json:"version"`
    Description string         `json:"description"`
    Speakers    []SpeakerEntry `json:"speakers"`
    lookup      map[string]bool
}

func LoadSpeakerAllowList(path string) (*SpeakerAllowList, error) {
    data, err := os.ReadFile(path)
    if err != nil {
        return nil, fmt.Errorf("load speaker allow-list: %w", err)
    }
    var al SpeakerAllowList
    if err := json.Unmarshal(data, &al); err != nil {
        return nil, fmt.Errorf("parse speaker allow-list: %w", err)
    }
    al.lookup = make(map[string]bool, len(al.Speakers))
    for _, s := range al.Speakers {
        al.lookup[strings.ToLower(s.Name)] = true
    }
    return &al, nil
}

func (al *SpeakerAllowList) IsAllowed(speaker string) bool {
    if al == nil || al.lookup == nil {
        return true // no allow-list loaded = allow all (backward compat)
    }
    return al.lookup[strings.ToLower(strings.TrimSpace(speaker))]
}
```

**Step 4: 테스트**

`workflow/internal/inkparse/speaker_allowlist_test.go`:
- TestLoadSpeakerAllowList: 임시 JSON 파일 생성 후 로드, speakers 수 확인
- TestIsAllowed: allow-list에 있는 화자 true, 없는 화자 false 반환
- TestIsAllowedNilList: nil SpeakerAllowList에서 true 반환 (backward compat)

**Step 5: 커버리지 보고서**

SUMMARY에 감사 결과 기록: total_dialogue, with_speaker, coverage_pct, 고유 화자 수, rejected 수.
coverage_pct 값을 명확히 기록 — Task 3의 실행 여부 판단 기준이 된다.
  </action>
  <verify>
    <automated>cd C:\Users\DELL\Desktop\localize-agent && go test ./workflow/internal/inkparse/ -run "TestLoadSpeakerAllowList|TestIsAllowed" -v</automated>
  </verify>
  <acceptance_criteria>
    - projects/esoteric-ebb/context/speaker_allow_list.json 파일 존재
    - speaker_allow_list.json에 "speakers" 배열과 "rejected" 배열 존재
    - workflow/internal/inkparse/speaker_allowlist.go에 `func LoadSpeakerAllowList(` 존재
    - workflow/internal/inkparse/speaker_allowlist.go에 `func (al *SpeakerAllowList) IsAllowed(` 존재
    - workflow/internal/inkparse/speaker_allowlist_test.go에 TestLoadSpeakerAllowList 존재
    - `go test ./workflow/internal/inkparse/` 통과
    - SUMMARY에 coverage_pct 값이 명시적으로 기록됨
  </acceptance_criteria>
  <done>화자 커버리지 비율이 측정되고, 검증된 allow-list JSON이 생성되고, Go 필터 함수가 테스트와 함께 구현됨</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 화자 커버리지 + allow-list 검증</name>
  <files>projects/esoteric-ebb/context/speaker_allow_list.json</files>
  <read_first>
    - projects/esoteric-ebb/context/speaker_allow_list.json (생성된 allow-list)
    - .planning/phases/06-foundation-cli/06-02-SUMMARY.md (감사 결과)
  </read_first>
  <action>
사용자가 화자 커버리지 감사 결과와 allow-list 내용을 검토한다.

확인 사항:
1. SUMMARY.md의 감사 결과 확인: total_dialogue, with_speaker, coverage_pct 값
2. coverage_pct가 90% 이상인지 확인 (D-06 목표)
3. `projects/esoteric-ebb/context/speaker_allow_list.json` 열어서:
   - speakers 배열에 주요 캐릭터 이름 + ability score (wis, str, int, cha, dex, con) 포함 확인
   - rejected 배열에 오인식 의심 항목 확인
4. rejected 항목 중 실제 화자인데 잘못 거부된 것이 없는지 확인

"approved" 또는 수정 사항 기술로 응답.
  </action>
  <verify>사용자가 "approved" 응답</verify>
  <acceptance_criteria>
    - 사용자가 allow-list 내용을 승인
    - coverage_pct >= 90% 확인 또는 미달 시 Task 3 실행 결정
  </acceptance_criteria>
  <done>사용자가 화자 allow-list와 커버리지 결과를 승인함</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: (조건부) 커버리지 90% 미달 시 isSpeakerTag 파서 강화</name>
  <files>workflow/internal/inkparse/parser.go, workflow/internal/inkparse/parser_test.go</files>
  <read_first>
    - workflow/internal/inkparse/parser.go (isSpeakerTag, isGameCommandTag 전체 로직)
    - workflow/internal/inkparse/parser_test.go (기존 isSpeakerTag 테스트)
    - projects/esoteric-ebb/context/speaker_allow_list.json (Task 1 생성 결과 — rejected 항목과 누락 패턴 분석)
    - .planning/phases/06-foundation-cli/06-02-SUMMARY.md (coverage_pct 확인)
  </read_first>
  <behavior>
    - Test 1: allow-list의 verified speaker 중 isSpeakerTag가 false를 반환하던 항목이 true로 변경
    - Test 2: isGameCommandTag가 true인 태그는 여전히 isSpeakerTag에서 false
    - Test 3: 기존 통과하던 speaker (Braxo, wis 등)가 여전히 true
    - Test 4: 강화 후 DB 재감사 시 coverage_pct가 이전보다 개선
  </behavior>
  <action>
per D-06, SPEAKER-02: 커버리지 90% 미달 시에만 이 태스크를 실행한다. 커버리지가 이미 90% 이상이면 SUMMARY에 "Task 3 스킵: coverage_pct >= 90%"로 기록하고 종료.

**실행 조건:** Task 1의 SUMMARY에서 coverage_pct < 90.0인 경우에만 실행.

**파서 강화 전략:**

1. Task 1에서 생성한 `speaker_allow_list.json`의 데이터를 분석:
   - `rejected` 항목 중 실제로는 speaker인데 `isSpeakerTag`가 false를 반환한 케이스 식별
   - `speaker == ''`인 대화 라인의 ink 소스를 샘플링하여, `#` 태그가 있는데 speaker로 인식 안 된 패턴 파악

2. 발견된 누락 패턴에 따라 `isSpeakerTag` 로직 강화. 가능한 강화 방향:
   - `isGameCommandTag`의 하드코딩 목록에서 실제 speaker가 잘못 포함된 경우 제거
   - 소문자 복합 단어 패턴 (현재 allLower 체크에서 걸리는 speaker)에 대한 예외 추가
   - allow-list를 `isSpeakerTag`에 직접 통합: allow-list에 있으면 즉시 true 반환 (최우선 체크)
   
   권장 접근: allow-list 통합이 가장 안전.
   ```go
   func isSpeakerTag(tag string) bool {
       // Priority 1: allow-list match (if loaded)
       if globalAllowList != nil && globalAllowList.IsAllowed(tag) {
           return true
       }
       // ... existing heuristic logic unchanged ...
   }
   ```
   
   globalAllowList는 패키지 레벨 변수로, 초기화 시 `SetSpeakerAllowList(*SpeakerAllowList)`로 설정.
   allow-list가 nil이면 기존 동작 유지 (backward compat).

3. 강화 후 검증:
   - 기존 parser_test.go의 isSpeakerTag 테스트가 모두 통과
   - 새 테스트: allow-list에 있는 speaker가 isSpeakerTag에서 true 반환
   - (가능하면) DB 재감사로 coverage_pct 개선 확인

주의: `isSpeakerTag`는 파서 내부 함수이므로 allow-list 통합 시 순환 의존성에 주의. `SpeakerAllowList` 타입이 같은 inkparse 패키지에 있으므로 문제 없음.
  </action>
  <verify>
    <automated>cd C:\Users\DELL\Desktop\localize-agent && go test ./workflow/internal/inkparse/ -run "TestIsSpeakerTag|TestLoadSpeakerAllowList|TestIsAllowed" -v</automated>
  </verify>
  <acceptance_criteria>
    - coverage_pct >= 90%인 경우: SUMMARY에 "Task 3 스킵" 기록, 파일 변경 없음
    - coverage_pct < 90%인 경우:
      - parser.go의 isSpeakerTag가 allow-list 통합 또는 패턴 추가로 강화됨
      - 기존 isSpeakerTag 테스트 모두 통과
      - 새 테스트에서 이전 누락 speaker가 true 반환
      - `go test ./workflow/internal/inkparse/` 통과
  </acceptance_criteria>
  <done>커버리지 90% 이상 달성 또는 이미 충족하여 스킵. SPEAKER-02 요구사항 완전 충족</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> allow-list JSON | DB 데이터가 정확하다고 가정 (v1.0에서 검증 완료) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | T (Tampering) | speaker_allow_list.json | accept | 버전 관리 대상 파일, git diff로 변경 추적 가능 |
| T-06-04 | S (Spoofing) | isSpeakerTag | mitigate | allow-list 필터로 오인식 차단. IsAllowed() false면 speaker 무시 |
</threat_model>

<verification>
```bash
cd C:\Users\DELL\Desktop\localize-agent && go test ./workflow/internal/inkparse/ -v
```
- allow-list JSON 파일이 projects/esoteric-ebb/context/ 에 존재
- LoadSpeakerAllowList + IsAllowed 테스트 통과
- 커버리지 비율이 SUMMARY에 기록
- 커버리지 90% 미달 시 isSpeakerTag 강화 후 테스트 통과
</verification>

<success_criteria>
- 화자 커버리지 비율이 측정되어 SUMMARY에 기록
- speaker_allow_list.json에 verified speakers + rejected 항목 포함
- LoadSpeakerAllowList, IsAllowed 함수 + 테스트 구현
- 사용자가 allow-list 내용을 승인
- 커버리지 90%+ 달성 (직접 달성 또는 파서 강화를 통해)
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-cli/06-02-SUMMARY.md`
</output>
