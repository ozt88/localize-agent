---
phase: 06-foundation-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflow/internal/clustertranslate/prompt.go
  - workflow/internal/clustertranslate/types.go
  - workflow/internal/clustertranslate/prompt_test.go
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-02
  - PROMPT-03

must_haves:
  truths:
    - "v2StaticRules 9개 규칙이 Context/Voice/Task/Constraints 4개 섹션으로 분류되어 있다"
    - "BuildBaseWarmup이 4섹션 구조로 워밍업을 조립한다"
    - "BuildScriptPrompt이 배치 내 speaker의 ability-score voice guide를 per-batch 프롬프트에 주입한다"
    - "프롬프트 토큰 예산이 PromptMeta에 기록되어 Phase 07 여유분 확인이 가능하다"
  artifacts:
    - path: "workflow/internal/clustertranslate/prompt.go"
      provides: "v2PromptSections struct + 4-tier prompt assembly + per-batch voice injection"
      contains: "v2PromptSections"
    - path: "workflow/internal/clustertranslate/types.go"
      provides: "PromptMeta에 EstimatedTokens 필드 추가"
      contains: "EstimatedTokens"
    - path: "workflow/internal/clustertranslate/prompt_test.go"
      provides: "프롬프트 계층화 + voice 주입 + 토큰 추정 테스트"
      contains: "TestBuildBaseWarmup"
  key_links:
    - from: "workflow/internal/clustertranslate/prompt.go"
      to: "v2PromptSections"
      via: "BuildBaseWarmup assembles sections"
      pattern: "v2Sections\\."
    - from: "workflow/internal/clustertranslate/prompt.go"
      to: "BuildScriptPrompt"
      via: "per-batch voice guide injection"
      pattern: "voiceGuide"
---

<objective>
v2StaticRules 9개 flat 규칙을 4-tier 섹션(Context/Voice/Task/Constraints)으로 계층화하고, per-batch ability-score voice guide를 주입하며, 프롬프트 토큰 예산을 프로파일링한다.

Purpose: Phase 07 컨텍스트 주입(voice card, branch context, continuity window)의 구조적 기반을 마련
Output: 계층화된 프롬프트 빌더 + voice 주입 + 토큰 추정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation-cli/06-CONTEXT.md
@.planning/phases/06-foundation-cli/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From workflow/internal/clustertranslate/types.go:
```go
type ClusterTask struct {
    Batch         inkparse.Batch
    PrevGateLines []string
    GlossaryJSON  string
}

type PromptMeta struct {
    LineCount        int
    ExcludedBlockIDs []string
    BlockIDOrder     []string
}
```

From workflow/internal/clustertranslate/prompt.go:
```go
var v2StaticRules = []string{
    "1. Translate the following scene into Korean.",
    "2. Maintain the [NN] line numbers exactly as given.",
    "3. Do not add, remove, or merge lines.",
    "4. Preserve speaker labels (e.g., 'Braxo:') in your output.",
    "5. Preserve [CHOICE] markers in your output.",
    "6. [CONTEXT] lines are for reference only -- do not translate them.",
    "7. All proper nouns (names, places, spells, abilities) stay in English.",
    "8. Match the tone and register of the original.",
    "9. Output only the translated lines, no commentary.",
}

func BuildBaseWarmup(systemPrompt, contextText, rulesText, glossaryWarmupJSON string) string
func BuildScriptPrompt(task ClusterTask) (string, PromptMeta)
func BuildContentSuffix(contentType string) string
```

From projects/esoteric-ebb/context/v2_base_prompt.md (ability-score voice guide 요약):
- wis: "침착하고 달관한 어조"
- str: "직선적이고 단순한 문장"
- int: "논리적이고 분석적인 화법"
- cha: "설득력 있고 감정이 풍부한 말투"
- dex: "간결하고 재치 있는 표현"
- con: "차분하고 인내심 있는 어조"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v2StaticRules를 v2PromptSections 4-tier 구조로 계층화</name>
  <files>workflow/internal/clustertranslate/prompt.go, workflow/internal/clustertranslate/prompt_test.go</files>
  <read_first>
    - workflow/internal/clustertranslate/prompt.go (현재 v2StaticRules + BuildBaseWarmup + BuildScriptPrompt 전체)
    - workflow/internal/clustertranslate/types.go (ClusterTask, PromptMeta)
    - projects/esoteric-ebb/context/v2_base_prompt.md (ability-score voice 정의 6개)
  </read_first>
  <behavior>
    - Test 1: v2Sections.Context에 "[CONTEXT] lines are for reference only" 규칙 포함
    - Test 2: v2Sections.Voice에 "Match the tone and register" 규칙 포함
    - Test 3: v2Sections.Task에 "Translate the following scene", "Preserve speaker labels", "Preserve [CHOICE] markers" 3개 규칙 포함
    - Test 4: v2Sections.Constraints에 "Maintain the [NN] line numbers", "Do not add, remove, or merge", "All proper nouns", "Output only" 4개 규칙 포함
    - Test 5: BuildBaseWarmup 출력에 "## Context", "## Voice", "## Task", "## Constraints" 4개 섹션 헤더가 순서대로 존재
    - Test 6: BuildBaseWarmup 출력이 모든 9개 규칙을 빠짐없이 포함
    - Test 7: sectionsToRules()가 9개 규칙을 올바른 순서로 반환 (기존 호출자 호환)
  </behavior>
  <action>
per D-01, D-02: `v2StaticRules` flat slice를 `v2PromptSections` struct로 교체.

1. prompt.go에 새 구조체 정의:
```go
type v2PromptSections struct {
    Context     []string
    Voice       []string
    Task        []string
    Constraints []string
}

var v2Sections = v2PromptSections{
    Context: []string{
        "[CONTEXT] lines are for reference only -- do not translate them.",
    },
    Voice: []string{
        "Match the tone and register of the original.",
    },
    Task: []string{
        "Translate the following scene into Korean.",
        "Preserve speaker labels (e.g., 'Braxo:') in your output.",
        "Preserve [CHOICE] markers in your output.",
    },
    Constraints: []string{
        "Maintain the [NN] line numbers exactly as given.",
        "Do not add, remove, or merge lines.",
        "All proper nouns (names, places, spells, abilities) stay in English.",
        "Output only the translated lines, no commentary.",
    },
}
```

2. `sectionsToRules()` 헬퍼 추가 — v2Sections를 flat []string으로 변환 (기존 `v2StaticRules` 사용처 호환):
```go
func sectionsToRules(s v2PromptSections) []string {
    var rules []string
    rules = append(rules, s.Context...)
    rules = append(rules, s.Voice...)
    rules = append(rules, s.Task...)
    rules = append(rules, s.Constraints...)
    return rules
}
```

3. `BuildBaseWarmup` 수정 — rulesPart 조립을 섹션별 마크다운 헤딩으로 변경:
```go
rulesPart := "## Translation Rules\n\n"
rulesPart += "### Context\n" + strings.Join(v2Sections.Context, "\n") + "\n\n"
rulesPart += "### Voice\n" + strings.Join(v2Sections.Voice, "\n") + "\n\n"
rulesPart += "### Task\n" + strings.Join(v2Sections.Task, "\n") + "\n\n"
rulesPart += "### Constraints\n" + strings.Join(v2Sections.Constraints, "\n")
```

4. 기존 `var v2StaticRules` 변수 제거. 내부에서 `v2StaticRules`를 참조하는 곳이 있으면 `sectionsToRules(v2Sections)`로 교체.

주의: `BuildScriptPrompt`은 `v2StaticRules`를 직접 참조하지 않으므로 변경 불필요. `BuildBaseWarmup`만 수정.
  </action>
  <verify>
    <automated>cd C:\Users\DELL\Desktop\localize-agent && go test ./workflow/internal/clustertranslate/ -run "TestBuildBaseWarmup|TestSectionsToRules|TestV2Sections" -v</automated>
  </verify>
  <acceptance_criteria>
    - prompt.go에 `type v2PromptSections struct` 정의 존재
    - prompt.go에 `var v2Sections = v2PromptSections{` 정의 존재
    - prompt.go에 `func sectionsToRules(` 정의 존재
    - prompt.go에 `var v2StaticRules` 가 더 이상 존재하지 않음
    - BuildBaseWarmup 출력에 "### Context", "### Voice", "### Task", "### Constraints" 포함
    - prompt_test.go에 TestBuildBaseWarmup, TestSectionsToRules 테스트 존재
    - `go test ./workflow/internal/clustertranslate/` 통과
  </acceptance_criteria>
  <done>v2StaticRules 9개 규칙이 4-tier 섹션으로 분류되고, BuildBaseWarmup이 섹션별 헤딩으로 조립하며, 모든 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-batch ability-score voice guide 주입 + 토큰 프로파일링</name>
  <files>workflow/internal/clustertranslate/prompt.go, workflow/internal/clustertranslate/types.go, workflow/internal/clustertranslate/prompt_test.go</files>
  <read_first>
    - workflow/internal/clustertranslate/prompt.go (Task 1 수정 후 상태)
    - workflow/internal/clustertranslate/types.go (ClusterTask, PromptMeta 현재 구조)
    - projects/esoteric-ebb/context/v2_base_prompt.md (ability-score voice guide 전문)
  </read_first>
  <behavior>
    - Test 1: speaker가 "wis"인 블록이 있는 배치에서 BuildScriptPrompt 출력에 "침착하고 달관한" 포함
    - Test 2: speaker가 "str"인 블록이 있는 배치에서 "직선적이고 단순한" 포함
    - Test 3: ability-score에 해당하지 않는 speaker (e.g., "Braxo")만 있으면 voice section 미생성
    - Test 4: 같은 배치에 "wis"와 "cha" 두 speaker가 있으면 두 guide 모두 포함, 중복 없음
    - Test 5: PromptMeta.EstimatedTokens가 0보다 큰 정수 반환
    - Test 6: estimateTokens("hello") == 1 (5/4=1), estimateTokens("안녕하세요") == 2 (5 runes / 2 = 2)
  </behavior>
  <action>
per D-03: ability-score voice guide를 per-batch 프롬프트에 주입. 워밍업에는 전체 가이드가 이미 포함(v2_base_prompt.md 경유)되므로, per-batch에서는 "이 배치에서 주의할 화자" 리마인더 형태로 짧게 주입 (Pitfall 4 방지).

1. prompt.go에 ability-score voice guide map 상수 추가:
```go
var abilityScoreVoice = map[string]string{
    "wis": "침착하고 달관한 어조, 지혜로운 조언자의 말투",
    "str": "직선적이고 단순한 문장, 힘 있는 화자",
    "int": "논리적이고 분석적인 화법, 학구적 어조",
    "cha": "설득력 있고 감정이 풍부한 말투, 매력적 화자",
    "dex": "간결하고 재치 있는 표현, 기민한 화자",
    "con": "차분하고 인내심 있는 어조, 꿋꿋한 화자",
}
```
NOTE: v2_base_prompt.md에서 정확한 한국어 텍스트를 복사할 것. 위는 RESEARCH.md 요약이므로 원문 확인 필수.

2. `buildVoiceSection` 함수 추가:
```go
func buildVoiceSection(speakers []string) string {
    seen := make(map[string]bool)
    var sb strings.Builder
    for _, s := range speakers {
        lower := strings.ToLower(strings.TrimSpace(s))
        guide, ok := abilityScoreVoice[lower]
        if ok && !seen[lower] {
            seen[lower] = true
            sb.WriteString(fmt.Sprintf("- **%s**: %s\n", lower, guide))
        }
    }
    if sb.Len() == 0 {
        return ""
    }
    return "\n## Voice Guide (이 배치의 화자)\n" + sb.String()
}
```

3. `BuildScriptPrompt` 수정 — 프롬프트 끝(glossary/suffix 이전)에 voice section 삽입:
```go
// 배치 내 speaker 수집
var speakers []string
for _, block := range translatableBlocks {
    if block.Speaker != "" {
        speakers = append(speakers, block.Speaker)
    }
}
voiceSection := buildVoiceSection(speakers)
if voiceSection != "" {
    prompt.WriteString(voiceSection)
}
```

4. types.go의 `PromptMeta`에 `EstimatedTokens int` 필드 추가.

5. prompt.go에 `estimateTokens` 함수 추가:
```go
func estimateTokens(text string) int {
    runes := []rune(text)
    koreanCount := 0
    for _, r := range runes {
        if r >= 0xAC00 && r <= 0xD7AF {
            koreanCount++
        }
    }
    englishChars := len(runes) - koreanCount
    return englishChars/4 + koreanCount/2
}
```

6. `BuildScriptPrompt` 반환 직전에 토큰 추정 기록:
```go
promptStr := prompt.String()
meta.EstimatedTokens = estimateTokens(promptStr)
return promptStr, meta
```

per PROMPT-03: 토큰 프로파일링은 PromptMeta.EstimatedTokens로 기록. Phase 07에서 예산 상한/우선순위 결정 시 이 값을 집계하여 사용.
  </action>
  <verify>
    <automated>cd C:\Users\DELL\Desktop\localize-agent && go test ./workflow/internal/clustertranslate/ -run "TestBuildScriptPrompt|TestBuildVoiceSection|TestEstimateTokens" -v</automated>
  </verify>
  <acceptance_criteria>
    - prompt.go에 `var abilityScoreVoice = map[string]string{` 존재
    - prompt.go에 `func buildVoiceSection(speakers []string) string` 존재
    - prompt.go에 `func estimateTokens(text string) int` 존재
    - types.go의 PromptMeta에 `EstimatedTokens int` 필드 존재
    - BuildScriptPrompt 내부에서 buildVoiceSection 호출
    - prompt_test.go에 TestBuildVoiceSection, TestEstimateTokens 테스트 존재
    - `go test ./workflow/internal/clustertranslate/` 통과
  </acceptance_criteria>
  <done>ability-score speaker가 배치에 포함되면 voice guide 리마인더가 per-batch 프롬프트에 주입되고, PromptMeta.EstimatedTokens에 토큰 근사치가 기록됨</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v2_base_prompt.md -> abilityScoreVoice | 프롬프트 텍스트가 하드코딩되므로 소스 불일치만 주의 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T (Tampering) | abilityScoreVoice map | accept | 하드코딩 상수, 런타임 변경 불가. 소스와 불일치 시 v2_base_prompt.md 참조하여 수정 |
| T-06-02 | I (Information Disclosure) | estimateTokens | accept | 토큰 카운트는 비밀 정보 아님 |
</threat_model>

<verification>
```bash
cd C:\Users\DELL\Desktop\localize-agent && go test ./workflow/internal/clustertranslate/ -v
```
- 모든 테스트 통과
- v2StaticRules 변수가 prompt.go에 존재하지 않음
- v2PromptSections 구조체와 v2Sections 변수가 존재
- BuildScriptPrompt 출력에 voice guide가 조건부 포함
</verification>

<success_criteria>
- v2StaticRules 9개 규칙이 Context(1)/Voice(1)/Task(3)/Constraints(4) 4개 섹션으로 분류
- BuildBaseWarmup 워밍업에 섹션별 헤딩이 포함
- ability-score speaker 배치에서 per-batch voice guide 주입
- PromptMeta.EstimatedTokens에 토큰 근사치 기록
- 모든 기존 + 신규 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation-cli/06-01-SUMMARY.md`
</output>
