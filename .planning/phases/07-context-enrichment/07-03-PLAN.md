---
phase: 07-context-enrichment
plan: 03
type: execute
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - workflow/internal/clustertranslate/prompt.go
  - workflow/internal/clustertranslate/prompt_test.go
  - workflow/internal/v2pipeline/worker.go
autonomous: false
requirements: [TONE-02, BRANCH-02, BRANCH-03, CONT-01, CONT-02]

must_haves:
  truths:
    - "BuildScriptPrompt가 voice card, branch context, next lines, prevKO/nextKO를 프롬프트에 주입한다"
    - "토큰 예산 초과 시 우선순위(D-08)에 따라 continuity -> branch -> voice card 순으로 축소된다"
    - "worker.go의 translateBatch가 voice card 로드, GetNextLines, GetAdjacentKO를 호출하여 ClusterTask를 완전히 조합한다"
    - "A/B 테스트에서 컨텍스트 주입 후 점수가 주입 전 대비 하락하지 않는다"
  artifacts:
    - path: "workflow/internal/clustertranslate/prompt.go"
      provides: "확장된 BuildScriptPrompt (voice + branch + continuity + budget)"
      contains: "Named Character Voice Guide"
    - path: "workflow/internal/v2pipeline/worker.go"
      provides: "translateBatch에서 확장된 ClusterTask 조합"
      contains: "GetNextLines"
  key_links:
    - from: "workflow/internal/v2pipeline/worker.go"
      to: "workflow/internal/clustertranslate/prompt.go"
      via: "BuildScriptPrompt(task) 호출"
      pattern: "clustertranslate\\.BuildScriptPrompt"
    - from: "workflow/internal/v2pipeline/worker.go"
      to: "workflow/internal/v2pipeline/store.go"
      via: "store.GetNextLines, store.GetAdjacentKO 호출"
      pattern: "store\\.GetNextLines|store\\.GetAdjacentKO"
    - from: "workflow/internal/clustertranslate/prompt.go"
      to: "workflow/internal/clustertranslate/voice_card.go"
      via: "BuildNamedVoiceSection 호출"
      pattern: "BuildNamedVoiceSection"
---

<objective>
프롬프트 통합 주입 + 토큰 예산 관리 + A/B 테스트: BuildScriptPrompt에 voice card, branch context, next lines, prevKO/nextKO 주입 확장 + worker.go에서 데이터 조합 + 토큰 예산 초과 시 D-08 우선순위 축소 + 저품질 배치 10개 A/B 비교

Purpose: Plan 01/02에서 준비한 데이터 소스(voice card, ParentChoiceText, GetNextLines, GetAdjacentKO)를 실제 번역 프롬프트에 주입하고, 토큰 예산 관리를 구현하며, A/B 테스트로 효과를 검증한다.
Output: 확장된 prompt.go, 확장된 worker.go, A/B 테스트 결과 확인
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-context-enrichment/07-CONTEXT.md
@.planning/phases/07-context-enrichment/07-01-SUMMARY.md
@.planning/phases/07-context-enrichment/07-02-SUMMARY.md

@workflow/internal/clustertranslate/prompt.go
@workflow/internal/clustertranslate/types.go
@workflow/internal/clustertranslate/voice_card.go
@workflow/internal/v2pipeline/worker.go
@workflow/internal/v2pipeline/store.go

<interfaces>
<!-- Interfaces from Plan 01 -->
From workflow/internal/clustertranslate/voice_card.go (created in Plan 01):
```go
type VoiceCard struct {
    Name        string `json:"name"`
    SpeechStyle string `json:"speech_style"`
    Honorific   string `json:"honorific"`
    Personality string `json:"personality"`
}

func LoadVoiceCards(path string) (map[string]VoiceCard, error)
func BuildNamedVoiceSection(speakers []string, cards map[string]VoiceCard) string
```

<!-- Interfaces from Plan 02 -->
From workflow/internal/clustertranslate/types.go (modified in Plan 02):
```go
type ClusterTask struct {
    Batch            inkparse.Batch
    PrevGateLines    []string
    GlossaryJSON     string
    NextLines        []string          // next 3줄 EN context
    PrevKO           []string          // prev KO translations
    NextKO           []string          // next KO translations
    VoiceCards       map[string]string // speaker -> voice guide text
    ParentChoiceText string            // parent choice display text
}
```

From workflow/internal/contracts/v2pipeline.go (modified in Plan 02):
```go
GetNextLines(knot, currentGate string, limit int) ([]string, error)
GetAdjacentKO(knot string, minSort, maxSort int, limit int) ([]string, []string, error)
```

<!-- Existing interfaces -->
From workflow/internal/clustertranslate/prompt.go:
```go
func estimateTokens(text string) int
func buildVoiceSection(speakers []string) string  // ability-score voice
func BuildScriptPrompt(task ClusterTask) (string, PromptMeta)
```

From workflow/internal/v2pipeline/worker.go (translateBatch, line ~108):
```go
task := clustertranslate.ClusterTask{
    Batch:         batch,
    GlossaryJSON:  glossaryJSON,
    PrevGateLines: prevGateLines,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BuildScriptPrompt 확장 (voice card + branch + next lines + prevKO/nextKO + 토큰 예산 관리)</name>
  <files>workflow/internal/clustertranslate/prompt.go, workflow/internal/clustertranslate/prompt_test.go</files>
  <read_first>
    - workflow/internal/clustertranslate/prompt.go (BuildScriptPrompt 전체, buildVoiceSection, estimateTokens)
    - workflow/internal/clustertranslate/prompt_test.go (기존 테스트 패턴)
    - workflow/internal/clustertranslate/types.go (ClusterTask 확장 필드 확인)
    - workflow/internal/clustertranslate/voice_card.go (BuildNamedVoiceSection 시그니처)
  </read_first>
  <behavior>
    - Test 1: ParentChoiceText가 있으면 [CONTEXT] 블록에 `Player chose: "텍스트"` 가 포함
    - Test 2: ParentChoiceText가 비어있으면 "Player chose" 가 출력에 미포함
    - Test 3: NextLines가 있으면 [CONTEXT] 블록에 `[N1]`, `[N2]` 번호로 next lines 포함
    - Test 4: PrevKO가 있으면 [CONTEXT] 블록에 `[K1]` 번호로 이전 한국어 번역 포함
    - Test 5: NextKO가 있으면 [CONTEXT] 블록에 `[NK1]` 번호로 다음 한국어 번역 포함
    - Test 6: VoiceCards에 매칭되는 speaker가 있으면 "## Named Character Voice Guide" 섹션 포함
    - Test 7: 토큰 예산 초과 시 continuity(NextLines) 먼저 삭제, 그 다음 branch(ParentChoiceText), voice card는 마지막
    - Test 8: 모든 컨텍스트가 있는 full 프롬프트에서 EstimatedTokens가 정확히 계산됨
  </behavior>
  <action>
per D-06/D-07/D-08 + TONE-02 + BRANCH-02/BRANCH-03 + CONT-01/CONT-02:

**BuildScriptPrompt 확장** (prompt.go):

기존 BuildScriptPrompt 함수를 다음과 같이 확장:

1. **[CONTEXT] 블록 확장** -- 기존 PrevGateLines 처리 후에 추가:

```go
// Branch context: "Player chose: X" (D-04, BRANCH-02)
if task.ParentChoiceText != "" {
    prompt.WriteString(fmt.Sprintf("[CONTEXT] Player chose: %q\n", task.ParentChoiceText))
}

// Next lines context (CONT-01)
if len(task.NextLines) > 0 {
    prompt.WriteString("[CONTEXT]\n")
    prompt.WriteString("(다음 게이트 -- 번역하지 마세요)\n")
    for i, line := range task.NextLines {
        fmt.Fprintf(&prompt, "[N%d] %q\n", i+1, line)
    }
}

// PrevKO context (CONT-02, 재번역 시)
if len(task.PrevKO) > 0 {
    prompt.WriteString("[CONTEXT]\n")
    prompt.WriteString("(이전 한국어 번역 -- 참고용)\n")
    for i, ko := range task.PrevKO {
        fmt.Fprintf(&prompt, "[K%d] %q\n", i+1, ko)
    }
}

// NextKO context (CONT-02, 재번역 시)
if len(task.NextKO) > 0 {
    prompt.WriteString("[CONTEXT]\n")
    prompt.WriteString("(다음 한국어 번역 -- 참고용)\n")
    for i, ko := range task.NextKO {
        fmt.Fprintf(&prompt, "[NK%d] %q\n", i+1, ko)
    }
}
```

전체 [CONTEXT] 블록(PrevGateLines + ParentChoiceText + NextLines + PrevKO + NextKO)을 하나의 `---` 구분자로 감싸기.

2. **Voice section 확장** -- 기존 buildVoiceSection(ability-score) 뒤에 named character voice 추가:

```go
// Named character voice cards (TONE-02)
if len(task.VoiceCards) > 0 {
    var namedSpeakers []string
    for _, block := range translatableBlocks {
        if block.Speaker != "" {
            namedSpeakers = append(namedSpeakers, block.Speaker)
        }
    }
    // Build inline voice guide from VoiceCards map
    seen := make(map[string]bool)
    var namedVoice strings.Builder
    for _, s := range namedSpeakers {
        if guide, ok := task.VoiceCards[s]; ok && !seen[s] {
            seen[s] = true
            fmt.Fprintf(&namedVoice, "- **%s**: %s\n", s, guide)
        }
    }
    if namedVoice.Len() > 0 {
        prompt.WriteString("\n## Named Character Voice Guide\n")
        prompt.WriteString(namedVoice.String())
    }
}
```

3. **토큰 예산 관리 함수** -- 새로운 내부 함수 `trimContextForBudget`:

```go
// trimContextForBudget removes context elements when estimated tokens exceed budget.
// Priority (D-08): voice card (last removed) > branch > continuity (first removed).
// maxTokens is the budget limit. Returns modified task.
func trimContextForBudget(task ClusterTask, maxTokens int) ClusterTask {
    // Estimate current prompt tokens
    testPrompt, _ := buildScriptPromptCore(task)
    tokens := estimateTokens(testPrompt)
    if tokens <= maxTokens {
        return task
    }

    // Phase 1: Remove continuity (lowest priority)
    task.NextLines = nil
    task.NextKO = nil
    task.PrevKO = nil
    testPrompt, _ = buildScriptPromptCore(task)
    if estimateTokens(testPrompt) <= maxTokens {
        return task
    }

    // Phase 2: Remove branch context
    task.ParentChoiceText = ""
    testPrompt, _ = buildScriptPromptCore(task)
    if estimateTokens(testPrompt) <= maxTokens {
        return task
    }

    // Phase 3: Remove voice cards (last resort)
    task.VoiceCards = nil
    return task
}
```

BuildScriptPrompt 내부에서 프롬프트 빌드 전에 `trimContextForBudget` 호출. maxTokens는 상수 `const contextBudgetTokens = 4000` (Claude's Discretion: gpt-5.4 context window 대비 프롬프트 50% 기준, 기존 프롬프트 평균 ~2000 + 최대 900 추가 = ~3000, 여유분 포함 4000).

실제 구현: BuildScriptPrompt의 핵심 로직을 내부 함수 `buildScriptPromptCore(task)` 로 분리하고, BuildScriptPrompt가 trimContextForBudget 후 buildScriptPromptCore를 호출하도록 리팩터.

**주의:**
- [CONTEXT] 블록의 순서: PrevGateLines -> ParentChoiceText -> NextLines -> PrevKO -> NextKO
- ability-score voice (buildVoiceSection)와 named character voice (VoiceCards)는 별도 섹션으로 유지
- PromptMeta.EstimatedTokens는 최종 프롬프트(trim 후) 기준으로 계산
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && go test ./workflow/internal/clustertranslate/ -run "TestBuildScriptPrompt|TestTrimContext" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - prompt.go에 `Player chose:` 문자열이 ParentChoiceText 주입에 사용됨
    - prompt.go에 `[N1]`, `[N2]` 패턴으로 NextLines 주입 존재
    - prompt.go에 `[K1]`, `[NK1]` 패턴으로 PrevKO/NextKO 주입 존재
    - prompt.go에 `Named Character Voice Guide` 섹션 존재
    - prompt.go에 `trimContextForBudget` 또는 동등한 토큰 예산 관리 함수 존재
    - prompt.go에 `contextBudgetTokens` 상수 정의 존재
    - prompt_test.go에 8개 이상의 신규 테스트 존재
    - `go test ./workflow/internal/clustertranslate/` 전체 통과
  </acceptance_criteria>
  <done>BuildScriptPrompt가 5종 컨텍스트를 주입하고, 토큰 예산 초과 시 D-08 우선순위로 축소하며, 8개 신규 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: worker.go translateBatch에서 확장된 ClusterTask 조합</name>
  <files>workflow/internal/v2pipeline/worker.go</files>
  <read_first>
    - workflow/internal/v2pipeline/worker.go (translateBatch 전체, 특히 ClusterTask 조합 부분 line 72-114)
    - workflow/internal/v2pipeline/store.go (GetPrevGateLines, GetNextLines, GetAdjacentKO)
    - workflow/internal/clustertranslate/types.go (ClusterTask 확장 필드)
    - workflow/internal/clustertranslate/voice_card.go (LoadVoiceCards, BuildNamedVoiceSection, VoiceCard)
  </read_first>
  <action>
per D-06/D-07/D-09 + TONE-02:

**Step 1: translateBatch에서 ClusterTask 조합 확장** (worker.go)

현재 코드 (line ~102-112):
```go
var prevGateLines []string
if items[0].Gate != "" {
    prevGateLines, _ = store.GetPrevGateLines(items[0].Knot, items[0].Gate, 3)
}

task := clustertranslate.ClusterTask{
    Batch:         batch,
    GlossaryJSON:  glossaryJSON,
    PrevGateLines: prevGateLines,
}
```

변경 후:
```go
var prevGateLines []string
if items[0].Gate != "" {
    prevGateLines, _ = store.GetPrevGateLines(items[0].Knot, items[0].Gate, 3)
}

// Next lines context (CONT-01, D-06)
var nextLines []string
if items[0].Gate != "" {
    nextLines, _ = store.GetNextLines(items[0].Knot, items[0].Gate, 3)
}

// PrevKO/NextKO for retranslation context (CONT-02, D-07)
var prevKO, nextKO []string
if items[0].RetranslationGen > 0 && len(items) > 0 {
    minSort := items[0].SortIndex
    maxSort := items[len(items)-1].SortIndex
    prevKO, nextKO, _ = store.GetAdjacentKO(items[0].Knot, minSort, maxSort, 3)
}

// ParentChoiceText from first block (BRANCH-01)
parentChoiceText := ""
if len(blocks) > 0 && blocks[0].Choice != "" {
    parentChoiceText = blocks[0].ParentChoiceText
}

// Voice cards (TONE-02) -- loaded at worker level, passed via cfg or parameter
// VoiceCards are pre-formatted: map[speaker]"speech_style, honorific, personality"
voiceCards := cfg.VoiceCards // map[string]string, loaded at startup

task := clustertranslate.ClusterTask{
    Batch:            batch,
    GlossaryJSON:     glossaryJSON,
    PrevGateLines:    prevGateLines,
    NextLines:        nextLines,
    PrevKO:           prevKO,
    NextKO:           nextKO,
    VoiceCards:       voiceCards,
    ParentChoiceText: parentChoiceText,
}
```

**Step 2: Config에 VoiceCards 필드 추가**

v2pipeline/types.go (또는 Config가 정의된 파일)에:
```go
VoiceCards map[string]string // speaker -> formatted voice guide text
```

**Step 3: worker 시작점에서 voice card 로드**

TranslateWorker 함수 또는 main.go에서 Config 초기화 시:
```go
// Load voice cards at startup
cards, err := clustertranslate.LoadVoiceCards(cfg.VoiceCardsPath)
if err != nil {
    fmt.Fprintf(os.Stderr, "warning: voice cards load failed: %v\n", err)
}
if cards != nil {
    cfg.VoiceCards = make(map[string]string)
    for name, card := range cards {
        cfg.VoiceCards[name] = fmt.Sprintf("%s, %s, %s", card.SpeechStyle, card.Honorific, card.Personality)
    }
}
```

또는 Config 초기화를 main.go의 Run 함수에서 수행. 기존 패턴에 맞추어 `cfg.VoiceCardsPath` 플래그 추가. project.json에 `voice_cards_path` 필드 추가.

**Step 4: ParentChoiceText가 블록에서 ClusterTask로 전달되는 경로**

translateBatch에서 blocks를 만들 때:
```go
blocks[i] = inkparse.DialogueBlock{
    ID:               item.ID,
    Text:             item.SourceRaw,
    Speaker:          item.Speaker,
    Choice:           item.Choice,
    ParentChoiceText: item.ParentChoiceText, // V2PipelineItem에도 필드 추가 필요
}
```

V2PipelineItem에 ParentChoiceText 필드가 없으면:
- 대안 1: V2PipelineItem에 필드 추가 + DB 컬럼 추가 (스키마 변경)
- 대안 2: ink 파서 결과에서 ParentChoiceText를 별도 룩업 (DB 조회 없이 batch의 choice 필드로 판단)
- 권장: V2PipelineItem/DB에 `parent_choice_text` 컬럼 추가 (Seed 시 inkparse 결과에서 전달). 스키마 마이그레이션은 ALTER TABLE ADD COLUMN으로 기존 데이터 호환.

DB 스키마에 `parent_choice_text TEXT NOT NULL DEFAULT ''` 추가:
- sqliteV2Schema에 ALTER TABLE 또는 CREATE TABLE에 추가
- postgresV2Schema에 동일 추가
- Seed 함수에서 item.ParentChoiceText 삽입

**주의:**
- RetranslationGen > 0 일 때만 PrevKO/NextKO 조회 (최초 번역 시 next는 미번역)
- voice card는 워커 시작 시 1회 로드, 매 배치마다 재로드하지 않음
- ParentChoiceText의 배치 내 여러 블록이 다른 choice에 속할 수 있으므로, 첫 블록의 choice가 비어있지 않을 때만 전달 (같은 gate/choice로 그룹된 배치이므로 대표값 사용)
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && go build ./workflow/... && go test ./workflow/internal/v2pipeline/ -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - worker.go에 `store.GetNextLines` 호출 존재
    - worker.go에 `store.GetAdjacentKO` 호출 존재 (RetranslationGen > 0 조건)
    - worker.go에 `ParentChoiceText` 설정 존재
    - worker.go에 `cfg.VoiceCards` 참조 존재
    - ClusterTask 생성에 NextLines, PrevKO, NextKO, VoiceCards, ParentChoiceText 6개 필드 모두 포함
    - `go build ./workflow/...` 전체 빌드 성공
    - `go test ./workflow/internal/v2pipeline/` 전체 통과
  </acceptance_criteria>
  <done>translateBatch가 5종 컨텍스트를 완전히 조합하여 ClusterTask를 구성하고, 전체 빌드 + 테스트 통과</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: A/B 테스트 -- 컨텍스트 주입 전후 점수 비교 검증</name>
  <files>projects/esoteric-ebb/context/voice_cards.json</files>
  <action>
저품질 배치 10개(named 캐릭터 포함 2+, 분기 포함 2+, 일반 6개)를 선택하여 컨텍스트 주입 전후 번역 점수를 비교한다 (D-09). 배치 선택 기준: score_final이 가장 낮은 배치 중에서 named character speaker가 있는 배치 2개, choice가 있는 배치 2개, 나머지 6개는 일반 배치.
  </action>
  <what-built>
Phase 07 전체 컨텍스트 주입 시스템: voice card 생성 + inkparse ParentChoiceText + store GetNextLines/GetAdjacentKO + BuildScriptPrompt 확장 + worker translateBatch 통합 + 토큰 예산 관리

A/B 테스트 준비: 저품질 배치 10개(named 캐릭터 포함 2+, 분기 포함 2+, 일반 6개)를 컨텍스트 주입 전/후로 번역하여 score 비교 (D-09)
  </what-built>
  <how-to-verify>
1. 저품질 배치 10개를 선택하여 컨텍스트 주입 후 재번역 실행:
   ```
   go run ./workflow/cmd/go-translation-pipeline -project projects/esoteric-ebb/project.json -once -voice-cards projects/esoteric-ebb/context/voice_cards.json
   ```
2. 재번역 결과의 score_final을 확인:
   ```sql
   SELECT batch_id, AVG(score_final) as avg_score
   FROM pipeline_items_v2
   WHERE batch_id IN (선택된 10개 배치)
   GROUP BY batch_id;
   ```
3. 컨텍스트 주입 전 점수 vs 후 점수를 비교하여 하락이 없는지 확인
4. 프롬프트 크기가 급격히 증가하지 않았는지 PromptMeta.EstimatedTokens 확인
  </how-to-verify>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && go build ./workflow/... && echo "build OK"</automated>
  </verify>
  <done>A/B 테스트 10개 배치에서 컨텍스트 주입 후 점수가 주입 전 대비 하락하지 않음</done>
  <resume-signal>"approved" 또는 발견된 문제 기술</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| voice card -> prompt | voice card 내용이 프롬프트에 주입됨 |
| DB ko_formatted -> prompt | 기존 번역이 프롬프트에 참고용으로 포함 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Denial of Service | 토큰 예산 초과 | mitigate | trimContextForBudget으로 D-08 우선순위 축소, contextBudgetTokens 상수로 상한 관리 |
| T-07-06 | Information Disclosure | prevKO/nextKO | accept | 번역 결과물만 포함, 민감 정보 없음, 로컬 전용 |
</threat_model>

<verification>
1. `go test ./workflow/internal/clustertranslate/ -v` 전체 통과
2. `go build ./workflow/...` 전체 빌드 성공
3. `go test ./workflow/internal/v2pipeline/ -v` 전체 통과
4. A/B 테스트 결과 점수 하락 없음 확인 (human-verify)
</verification>

<success_criteria>
- BuildScriptPrompt가 voice card, branch context, next lines, prevKO/nextKO 5종 컨텍스트를 주입
- 토큰 예산 초과 시 D-08 우선순위로 자동 축소
- worker가 확장된 ClusterTask를 완전히 조합
- A/B 테스트 10개 배치에서 점수 하락 없음
- 전체 빌드 + 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/07-context-enrichment/07-03-SUMMARY.md`
</output>
