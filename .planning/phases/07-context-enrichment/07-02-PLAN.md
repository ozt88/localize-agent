---
phase: 07-context-enrichment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workflow/internal/inkparse/types.go
  - workflow/internal/inkparse/parser.go
  - workflow/internal/inkparse/parser_test.go
  - workflow/internal/v2pipeline/store.go
  - workflow/internal/v2pipeline/store_test.go
  - workflow/internal/contracts/v2pipeline.go
  - workflow/internal/clustertranslate/types.go
autonomous: true
requirements: [BRANCH-01, BRANCH-02, CONT-01, CONT-02]

must_haves:
  truths:
    - "DialogueBlock에 ParentChoiceText 필드가 존재하고, choice container 안의 블록에 부모 선택지 텍스트가 채워진다"
    - "store.GetNextLines가 현재 gate 이후 N줄의 source_raw를 반환한다"
    - "store.GetAdjacentKO가 배치 앞뒤 아이템의 ko_formatted를 반환한다"
    - "ClusterTask에 NextLines, PrevKO, NextKO, ParentChoiceText, VoiceCards 필드가 추가된다"
  artifacts:
    - path: "workflow/internal/inkparse/types.go"
      provides: "DialogueBlock.ParentChoiceText 필드"
      contains: "ParentChoiceText"
    - path: "workflow/internal/v2pipeline/store.go"
      provides: "GetNextLines, GetAdjacentKO 메서드"
      exports: ["GetNextLines", "GetAdjacentKO"]
    - path: "workflow/internal/contracts/v2pipeline.go"
      provides: "V2PipelineStore 인터페이스에 GetNextLines, GetAdjacentKO 추가"
      contains: "GetNextLines"
    - path: "workflow/internal/clustertranslate/types.go"
      provides: "ClusterTask 확장 필드"
      contains: "NextLines"
  key_links:
    - from: "workflow/internal/inkparse/parser.go"
      to: "workflow/internal/inkparse/types.go"
      via: "DialogueBlock.ParentChoiceText 설정"
      pattern: "ParentChoiceText"
    - from: "workflow/internal/v2pipeline/store.go"
      to: "workflow/internal/contracts/v2pipeline.go"
      via: "V2PipelineStore 인터페이스 구현"
      pattern: "GetNextLines|GetAdjacentKO"
---

<objective>
분기 맥락 추출 + 연속성 윈도우 확장: ink 파서에 ParentChoiceText 추출 추가, store에 GetNextLines/GetAdjacentKO 쿼리 추가, ClusterTask 확장

Purpose: Plan 03에서 프롬프트에 주입할 데이터 소스를 준비한다. (1) ink 파서가 choice container의 부모 선택지 텍스트를 추출하고, (2) DB 쿼리가 next 3줄과 prevKO/nextKO를 제공한다.
Output: inkparse ParentChoiceText 확장, store GetNextLines/GetAdjacentKO, ClusterTask 신규 필드
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-context-enrichment/07-CONTEXT.md

@workflow/internal/inkparse/parser.go
@workflow/internal/inkparse/types.go
@workflow/internal/v2pipeline/store.go
@workflow/internal/contracts/v2pipeline.go
@workflow/internal/clustertranslate/types.go

<interfaces>
<!-- Key types and contracts the executor needs -->

From workflow/internal/inkparse/types.go (current):
```go
type DialogueBlock struct {
    ID            string   `json:"id"`
    Path          string   `json:"path"`
    Text          string   `json:"text"`
    SourceHash    string   `json:"source_hash"`
    SourceFile    string   `json:"source_file"`
    Knot          string   `json:"knot"`
    Gate          string   `json:"gate"`
    Choice        string   `json:"choice"`
    Speaker       string   `json:"speaker"`
    Tags          []string `json:"tags"`
    BlockIndex    int      `json:"block_index"`
    ContentType   string   `json:"content_type"`
    IsPassthrough bool     `json:"is_passthrough"`
}
```

From workflow/internal/inkparse/parser.go (walkContainer signature):
```go
func (w *walker) walkContainer(arr []any, knot, gate, choice string)
func (w *walker) walkFlatContent(arr []any, knot, gate, choice string)
func (w *walker) tryExtractChoiceText(remaining []any, knot, gate string)
```
- walkContainer에서 c-N 서브컨테이너 진입 시: `w.walkContainer(subArr, knot, gate, key)` (key = "c-0", "c-1" 등)
- tryExtractChoiceText에서 "s" 배열의 extractTextFromArray로 선택지 텍스트 추출 후 DialogueBlock 생성

From workflow/internal/v2pipeline/store.go (GetPrevGateLines pattern):
```go
func (s *Store) GetPrevGateLines(knot, currentGate string, limit int) ([]string, error) {
    rows, err := s.db.Query(s.rebind(`
        SELECT source_raw FROM pipeline_items_v2
        WHERE knot = ? AND gate != ? AND gate != ''
          AND sort_index < (SELECT MIN(sort_index) FROM pipeline_items_v2 WHERE knot = ? AND gate = ?)
        ORDER BY sort_index DESC
        LIMIT ?`),
        knot, currentGate, knot, currentGate, limit)
    // ... scan lines ...
}
```

From workflow/internal/contracts/v2pipeline.go (V2PipelineStore interface):
```go
type V2PipelineStore interface {
    GetPrevGateLines(knot, currentGate string, limit int) ([]string, error)
    // ... other methods ...
}
```

From workflow/internal/clustertranslate/types.go (current ClusterTask):
```go
type ClusterTask struct {
    Batch         inkparse.Batch
    PrevGateLines []string
    GlossaryJSON  string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: inkparse ParentChoiceText 필드 추가 + 파서에서 부모 선택지 텍스트 전달</name>
  <files>workflow/internal/inkparse/types.go, workflow/internal/inkparse/parser.go, workflow/internal/inkparse/parser_test.go</files>
  <read_first>
    - workflow/internal/inkparse/types.go (DialogueBlock 현재 구조)
    - workflow/internal/inkparse/parser.go (walkContainer, walkFlatContent, tryExtractChoiceText 전체 흐름)
    - workflow/internal/inkparse/parser_test.go (기존 테스트 패턴)
  </read_first>
  <behavior>
    - Test 1: choice container 내부 블록에 ParentChoiceText가 해당 선택지 텍스트로 채워진다
    - Test 2: choice가 아닌 일반 gate 블록의 ParentChoiceText는 빈 문자열
    - Test 3: 중첩 choice (c-0 안에 c-1)의 경우 직계 부모 choice text만 전달 (깊이 1 제한 per D-05)
    - Test 4: DC/FC prefix가 있는 선택지 텍스트에서 prefix가 strip된 clean text가 ParentChoiceText에 들어간다
  </behavior>
  <action>
per D-04/D-05:

**Step 1: DialogueBlock에 ParentChoiceText 필드 추가** (types.go)
```go
ParentChoiceText string `json:"parent_choice_text,omitempty"` // 부모 선택지 텍스트 (분기 맥락)
```
IsPassthrough 필드 아래에 추가.

**Step 2: walker 구조체에 currentChoiceText 필드 추가** (parser.go)
walker 구조체에 추가:
```go
currentChoiceText string // 현재 choice container의 선택지 텍스트 (깊이 1, D-05)
```

**Step 3: walkContainer에서 c-N 진입 시 choiceText 설정**
parser.go의 walkContainer 메서드에서 sub-containers를 순회하는 map[string]any 블록 중 `strings.HasPrefix(key, "c-")` 분기:

현재 코드:
```go
if strings.HasPrefix(key, "c-") {
    w.walkContainer(subArr, knot, gate, key)
}
```

변경 후:
```go
if strings.HasPrefix(key, "c-") {
    // Extract choice display text for branch context (D-04)
    choiceText := w.extractChoiceDisplayText(subArr)
    oldChoiceText := w.currentChoiceText
    w.currentChoiceText = choiceText
    w.walkContainer(subArr, knot, gate, key)
    w.currentChoiceText = oldChoiceText // restore (D-05 깊이 1)
}
```

**Step 4: extractChoiceDisplayText 헬퍼 추가** (parser.go)
c-N 컨테이너의 배열에서 tryExtractChoiceText와 동일한 로직으로 "s" 배열의 display text를 추출하되, DialogueBlock을 생성하지 않고 텍스트만 반환:
```go
func (w *walker) extractChoiceDisplayText(arr []any) string {
    for _, elem := range arr {
        if dict, ok := elem.(map[string]any); ok {
            if _, ok := dict["*"]; ok {
                if flg, ok := dict["flg"].(float64); ok && int(flg)&0x2 != 0 {
                    if s, ok := dict["s"].([]any); ok {
                        text := extractTextFromArray(s)
                        if text != "" {
                            cleanText, _ := stripDCFCPrefix(text)
                            return cleanText
                        }
                    }
                }
            }
        }
    }
    return ""
}
```

**Step 5: flushBlock에서 ParentChoiceText 설정** (parser.go)
walkFlatContent 내부의 flushBlock 클로저에서 DialogueBlock 생성 시:
```go
block := DialogueBlock{
    // ... existing fields ...
    ParentChoiceText: w.currentChoiceText, // 추가
}
```

또한 tryExtractChoiceText에서 생성하는 DialogueBlock에도 동일하게:
```go
block := DialogueBlock{
    // ... existing fields ...
    ParentChoiceText: w.currentChoiceText, // choice 자체의 부모 텍스트
}
```

**주의:** walker 초기화 시 currentChoiceText = "" (기본값). choice가 아닌 gate 진입 시에는 설정하지 않으므로 자동으로 "".
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && go test ./workflow/internal/inkparse/ -run "TestParentChoiceText|TestParse" -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - types.go에 `ParentChoiceText string` 필드 존재 (json tag: `parent_choice_text,omitempty`)
    - parser.go에 `currentChoiceText string` 필드가 walker 구조체에 존재
    - parser.go에 `func (w *walker) extractChoiceDisplayText(arr []any) string` 함수 존재
    - parser.go의 c-N 분기에서 `w.currentChoiceText = choiceText` 설정 존재
    - flushBlock 내 DialogueBlock 생성에 `ParentChoiceText: w.currentChoiceText` 존재
    - parser_test.go에 ParentChoiceText 관련 테스트 4개 이상
    - `go test ./workflow/internal/inkparse/` 전체 통과
  </acceptance_criteria>
  <done>DialogueBlock.ParentChoiceText가 choice container 내 블록에 부모 선택지 텍스트를 정확히 채우고, 기존 테스트 + 신규 4개 테스트 통과</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: store GetNextLines/GetAdjacentKO + ClusterTask 필드 확장 + V2PipelineStore 인터페이스 확장</name>
  <files>workflow/internal/v2pipeline/store.go, workflow/internal/v2pipeline/store_test.go, workflow/internal/contracts/v2pipeline.go, workflow/internal/clustertranslate/types.go</files>
  <read_first>
    - workflow/internal/v2pipeline/store.go (GetPrevGateLines 패턴, rebind 메서드, sqliteV2Schema)
    - workflow/internal/v2pipeline/store_test.go (기존 테스트 패턴, 테스트 DB 설정)
    - workflow/internal/contracts/v2pipeline.go (V2PipelineStore 인터페이스 전체)
    - workflow/internal/clustertranslate/types.go (ClusterTask 현재 구조)
  </read_first>
  <behavior>
    - Test 1: GetNextLines returns next 3 source_raw after current gate (sort_index 기준)
    - Test 2: GetNextLines with empty knot/gate returns nil, nil
    - Test 3: GetNextLines with no items after current gate returns empty slice
    - Test 4: GetAdjacentKO returns prevKO/nextKO for items adjacent to given sort_index range
    - Test 5: GetAdjacentKO skips items with NULL ko_formatted (미번역 상태)
    - Test 6: GetAdjacentKO with no adjacent done items returns empty slices
  </behavior>
  <action>
per D-06/D-07 + CONT-01/CONT-02:

**Step 1: V2PipelineStore 인터페이스에 2개 메서드 추가** (contracts/v2pipeline.go)
Close() error 앞에 추가:
```go
// GetNextLines returns the first N source_raw texts after the current gate
// in the same knot, ordered by sort_index ascending. Used for continuity window (CONT-01).
GetNextLines(knot, currentGate string, limit int) ([]string, error)

// GetAdjacentKO returns ko_formatted texts for items adjacent to the given sort_index range.
// prevKO: items with sort_index < minSort, ordered DESC (closest first), limit N.
// nextKO: items with sort_index > maxSort, ordered ASC, limit N.
// Only returns items in state=done with non-NULL ko_formatted. Used for retranslation (CONT-02).
GetAdjacentKO(knot string, minSort, maxSort int, limit int) (prevKO []string, nextKO []string, err error)
```

**Step 2: Store에 GetNextLines 구현** (v2pipeline/store.go)
GetPrevGateLines 바로 아래에 추가. 동일 패턴으로 sort_index > MAX(current gate) 기준:
```go
func (s *Store) GetNextLines(knot, currentGate string, limit int) ([]string, error) {
    if knot == "" || currentGate == "" {
        return nil, nil
    }
    rows, err := s.db.Query(s.rebind(`
        SELECT source_raw FROM pipeline_items_v2
        WHERE knot = ? AND gate != ? AND gate != ''
          AND sort_index > (SELECT MAX(sort_index) FROM pipeline_items_v2 WHERE knot = ? AND gate = ?)
        ORDER BY sort_index ASC
        LIMIT ?`),
        knot, currentGate, knot, currentGate, limit,
    )
    if err != nil {
        return nil, err
    }
    defer rows.Close()
    var lines []string
    for rows.Next() {
        var line string
        if err := rows.Scan(&line); err != nil {
            return nil, err
        }
        lines = append(lines, line)
    }
    return lines, rows.Err()
}
```

**Step 3: Store에 GetAdjacentKO 구현** (v2pipeline/store.go)
GetNextLines 아래에 추가:
```go
func (s *Store) GetAdjacentKO(knot string, minSort, maxSort int, limit int) ([]string, []string, error) {
    if knot == "" {
        return nil, nil, nil
    }
    // Prev KO: items before minSort, closest first
    prevRows, err := s.db.Query(s.rebind(`
        SELECT ko_formatted FROM pipeline_items_v2
        WHERE knot = ? AND sort_index < ? AND state = 'done' AND ko_formatted IS NOT NULL AND ko_formatted != ''
        ORDER BY sort_index DESC
        LIMIT ?`),
        knot, minSort, limit,
    )
    if err != nil {
        return nil, nil, err
    }
    defer prevRows.Close()
    var prevKO []string
    for prevRows.Next() {
        var ko string
        if err := prevRows.Scan(&ko); err != nil {
            return nil, nil, err
        }
        prevKO = append(prevKO, ko)
    }
    if err := prevRows.Err(); err != nil {
        return nil, nil, err
    }

    // Next KO: items after maxSort, closest first
    nextRows, err := s.db.Query(s.rebind(`
        SELECT ko_formatted FROM pipeline_items_v2
        WHERE knot = ? AND sort_index > ? AND state = 'done' AND ko_formatted IS NOT NULL AND ko_formatted != ''
        ORDER BY sort_index ASC
        LIMIT ?`),
        knot, maxSort, limit,
    )
    if err != nil {
        return prevKO, nil, err
    }
    defer nextRows.Close()
    var nextKO []string
    for nextRows.Next() {
        var ko string
        if err := nextRows.Scan(&ko); err != nil {
            return prevKO, nil, err
        }
        nextKO = append(nextKO, ko)
    }
    return prevKO, nextKO, nextRows.Err()
}
```

**Step 4: ClusterTask 확장** (clustertranslate/types.go)
기존 3필드 뒤에 5개 필드 추가:
```go
type ClusterTask struct {
    Batch            inkparse.Batch
    PrevGateLines    []string          // prev 3줄 EN context (기존)
    GlossaryJSON     string            // per-batch glossary terms (기존)
    NextLines        []string          // next 3줄 EN context (CONT-01, 신규)
    PrevKO           []string          // prev 3줄 기존 KO 번역, 재번역 시 (CONT-02, 신규)
    NextKO           []string          // next 3줄 기존 KO 번역, 재번역 시 (CONT-02, 신규)
    VoiceCards       map[string]string // speaker -> voice guide text (TONE-02, 신규)
    ParentChoiceText string            // 부모 선택지 텍스트 (BRANCH-01, 신규)
}
```

**Step 5: 인터페이스 구현체 확인**
contracts/v2pipeline.go에 추가한 GetNextLines/GetAdjacentKO가 Store에 구현되었으므로, `var _ contracts.V2PipelineStore = (*Store)(nil)` 컴파일 타임 검사 통과. 다른 fake 구현체(테스트용)가 있다면 해당 파일에도 stub 추가 필요 -- store_test.go 내 fake에 추가.
  </action>
  <verify>
    <automated>cd C:/Users/DELL/Desktop/localize-agent && go test ./workflow/internal/v2pipeline/ -run "TestGetNextLines|TestGetAdjacentKO" -v -count=1 && go build ./workflow/...</automated>
  </verify>
  <acceptance_criteria>
    - contracts/v2pipeline.go에 `GetNextLines(knot, currentGate string, limit int) ([]string, error)` 시그니처 존재
    - contracts/v2pipeline.go에 `GetAdjacentKO(knot string, minSort, maxSort int, limit int) ([]string, []string, error)` 시그니처 존재
    - store.go에 `func (s *Store) GetNextLines` 구현 존재
    - store.go에 `func (s *Store) GetAdjacentKO` 구현 존재
    - clustertranslate/types.go의 ClusterTask에 `NextLines`, `PrevKO`, `NextKO`, `VoiceCards`, `ParentChoiceText` 필드 존재
    - store_test.go에 GetNextLines/GetAdjacentKO 테스트 6개 이상
    - `go build ./workflow/...` 전체 빌드 성공 (인터페이스 만족)
  </acceptance_criteria>
  <done>GetNextLines/GetAdjacentKO 쿼리가 구현되고, ClusterTask가 확장되고, V2PipelineStore 인터페이스가 확장되어 전체 빌드 통과</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB -> source_raw/ko_formatted | SQL injection 방지 (parameterized query 사용) |
| ink JSON -> ParentChoiceText | 파서 입력은 게임 원본 JSON, 신뢰 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | GetAdjacentKO | accept | ko_formatted는 번역 결과물, 민감 정보 아님 |
| T-07-04 | Tampering | ParentChoiceText | accept | ink JSON은 게임 원본, 로컬 전용 |
</threat_model>

<verification>
1. `go test ./workflow/internal/inkparse/ -v` 전체 통과
2. `go test ./workflow/internal/v2pipeline/ -run "TestGetNextLines|TestGetAdjacentKO" -v` 통과
3. `go build ./workflow/...` 전체 빌드 성공
</verification>

<success_criteria>
- DialogueBlock.ParentChoiceText가 choice container 내 블록에 부모 선택지 텍스트 전달
- GetNextLines/GetAdjacentKO가 sort_index 기반으로 정확한 데이터 반환
- ClusterTask가 5개 신규 필드로 확장
- 전체 빌드 + 테스트 통과
</success_criteria>

<output>
After completion, create `.planning/phases/07-context-enrichment/07-02-SUMMARY.md`
</output>
