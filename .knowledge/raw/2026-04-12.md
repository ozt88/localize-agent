### 00:36 — OpenCode 서버 hang → stale claims 복구, score worker 재시작
- working_score=20 항목 lease 5시간 초과 (19:25 만료) — OpenCode 서버 응답 불가가 원인
- --cleanup-stale-claims로 20개 pending_score로 복구, OpenCode restart (PID 510200), score worker 재시작 (PID 951)
- 현재: pending_score=64, working_score=11, score worker 정상 작동 중
- 다음: A/B 테스트 배치 스코어링 완료 대기 → No RAG 점수 수집

### 01:00 — score worker 성능 개선 4종 수정
- watchdog: 감지 주기 6분→2분, 재시작 후 stale claims 자동 reclaim 추가 (run.go)
- score sub-batch: 5→10 items/LLM call (worker.go)
- project.json: score concurrency 1→3, batch_size 8→10, timeout 120→180s
- 빌드 성공, working_score=29 확인 (3 워커 정상 처리 중)
- 예상 처리량 3× 개선

### 01:20 — EnsureContext 실패 시 working items 미해제 버그 수정
- 버그: translate/format/score 워커 모두 warmup(EnsureContext) 실패 시 UpdateRetryState 호출 없음
- 결과: OpenCode 다운 → warmup 에러 → items가 lease 만료까지(최대 400s) working 상태로 묶힘 (5시간 stuck의 실제 원인)
- 수정: 3곳 모두 warmup 에러 핸들러에 retry 로직 추가 (worker.go)
- 추가: restartOpenCode를 deep probe 대기로 변경 (TCP connect → API 준비 확인)
- concurrency 3→2 복원 (3이면 OpenCode 불안정)
- 변경 파일: workflow/internal/v2pipeline/worker.go, run.go, project.json

### 13:00 — settings.json bypassPermissions 설정
- permissions.defaultMode: "bypassPermissions" 추가 — 권한 확인 팝업 비활성화
- 최상위 defaultMode는 스키마 불허, permissions 객체 내부에 위치해야 함
- 변경 파일: C:\Users\DELL\.claude\settings.json

### — bypassPermissions VSCode 익스텐션 해결 + 파이프라인 재시작
- VSCode 익스텐션에서 bypassPermissions는 settings.json 외에 UI에서 별도 활성화 필요 (Dangerous 설정 + 채팅창 우하단 토글)
- 파이프라인 실패 원인: watchdog deepProbe가 서버 바쁠 때 timeout → false kill → 전체 연결 끊김
- 수정: deepProbe → probeServer (간단 HTTP GET)로 교체, retranslate.go 누락 메서드 구현으로 컴파일 에러 해결
- 파이프라인 재시작: done=3412, pending=30610, 정상 가동 중

### 14:00 — Phase 08 품질 실패 판정 → 전량 폐기 결정
- AR_Kattegatt 씬 비교: gen=0(구버전) `그대/~도다` 고어체 vs gen=1(신버전) `너/~다` 현대 반말로 평탄화
- 원인: 56fa1ea(08-01)에서 --voice-cards, --rag-context 플래그 삭제됨 → f00bbac에서 미복원
- 결정: gen=1 번역 전량 폐기, Phase 08을 "인프라 완료"로 재정의, 실번역은 Phase 09로 이월
- gen=0 done 6건 → pending_translate 리셋 (새 번역 규정 준수)

### 15:00 — Phase 08 GSD 갈무리 완료
- 08-02-SUMMARY.md 작성: L-01~L-04 교훈 4개 기록 (샘플 검토, main.go 플래그 회귀, 전제조건 체크리스트, voice card 필수)
- ROADMAP.md: Phase 08 CLOSED, Phase 09 3개 플랜 추가
- STATE.md: Phase 08 종료 사유 + Phase 09 진입 전 체크리스트 업데이트
- 커밋: f87d683

### 17:00 — Phase 09 discuss 시작 — 근본 원인 분석
- 56fa1ea가 삭제한 코드 전량 확인: types.go VoiceCardsPath/VoiceCards/RAGContextPath, worker.go voice card 로딩/nextLines/prevKO/nextKO/RAG 주입, main.go 플래그
- f00bbac는 파일(packages, data)은 복원했으나 통합 코드(worker.go, types.go, main.go)는 복원 안 함
- Gray Area 1 결정: 전량 복원 (Phase 07/07.1은 additive, 제거가 실수)
- voice_cards.json: git 히스토리에 15명 존재, Kattegatt 미포함. voice card 생성 CLI가 RAG 미활용이 구조적 문제
- Phase 09 방향: 좁은 Kattegatt 수동 수정이 아닌, voice card 생성 파이프라인 전체를 wiki/RAG 기반으로 개선하는 광범위한 방향으로 결정

### 19:00 — Phase 09 discuss 완료 — 09-CONTEXT.md 작성
- Gray Area 4(샘플 검증 씬): VL_Visken 선정 — Visken(pale mortician, voice card 보유)과 Ôst(kobold 가드 = 작은 도마뱀형 생물), 유저가 직접 플레이한 초반 씬
- 09-CONTEXT.md 작성 완료: 4개 Gray Area 결정 사항, 전제조건 체크리스트, relationships 필드 설계 포함
- 변경 파일: .planning/phases/09-retranslation-execution/09-CONTEXT.md (신규 생성)

### 18:00 — Phase 09 discuss — voice card 설계 확장 결정
- 현재 번역 프롬프트 컨텍스트 전체 파악: PrevGateLines, NextLines, PrevKO/NextKO, GlossaryJSON, VoiceCards, ParentChoiceText, RAGHints (ClusterTask 8개 필드)
- RAG가 빠진 유일한 소스: voice card 생성 (DB 샘플만 사용, wiki 미활용)
- 결정 1(Gray Area 2,3): voice card에 relationships 필드 추가 (Option A) — 화자뿐 아니라 청자 정보도 포함
- 결정 2: 프롬프트 빌드 시 배치 내 실제 등장 speakers로 relationships 필터링 → 토큰 압축
- 결정 3: go-generate-voice-cards CLI 개선 — wiki 캐릭터 페이지 + DB 공동 출현 화자 → LLM이 relationships까지 생성
- voice card 어미 예시 패턴: speech_style에 "어미: ~야, ~잖아" 형태로 구체적 예시 추가 (전체 15개 카드 + Kattegatt 포함)
- knowledge compiler 수집 불안정 문제 확인: CLAUDE.md 행동 지시가 skill 실행 중 밀림 → Stop Hook 저장 로직 미구현이 근본 원인
