### — CLAUDE.md 행동 규칙 추가
- 사용자 피드백: 도구로 확인 가능한 건 직접 확인, 사용자에게 묻지 말 것
- CLAUDE.md 상단에 "행동 규칙" 섹션 추가 (memory에만 있던 규칙을 CLAUDE.md에 반영)
- 변경 파일: `C:\Users\DELL\.claude\CLAUDE.md`

### — knowledge compiler 첫 번째 컴파일 실행
- raw/ → knowledge/ 수동 컴파일 (첫 번째 컴파일, 이전 knowledge/ 비어있었음)
- 생성: decisions.md(3건), guardrails.md(3건), troubleshooting.md(4건), index.md
- 핵심 내용: PageIndex 거절 결정, applyProjectDefaults sqlite 버그, A/B 테스트 concurrency 부족 패턴

### — ab_test_rag.py stale working_translate 버그 수정
- 근본 원인: (1) --once 플래그가 빈 큐에서 idle sleep 반복, 자연 종료 없이 300초 타임아웃으로 강제 kill → working 아이템 고착. (2) run_pipeline_stage() 완료 조건이 pending=0만 체크하고 working=0 미확인 → 2/10 배치만 스코어됨
- 수정: 완료 조건에 working_{stage}=0 추가, pending=0 but working>0 시 --lease-sec 1로 강제 cleanup, pass_timeout=180/lease_sec=150 명시, max_passes=30 상향
- 변경 파일: `projects/esoteric-ebb/context/ab_test_rag.py`

### — knowledge compiler Step 0 미동작 원인 분석 및 패치 개선
- 원인: gsd-phase-researcher 에이전트가 Step 0 분석은 수행했으나 Write tool 호출 건너뜀 (LLM 지시 이행 신뢰성 문제)
- 개선: 패치 7/8번 단계에 Write tool 명시적 호출 요구 + Step 0 completion gate 추가
- 변경 파일: `knowledge-compiler/patches/gsd-phase-researcher.patch.md`, `~/.claude/agents/gsd-phase-researcher.md` (재적용)

### — Phase 07.1 Plan 04 A/B 테스트 클린 재실행 결과: FAIL
- 결과: avg delta = -0.81 (threshold >= -0.5), Verdict: FAIL, Scored batches: 10/10
- 발견: No RAG baseline이 구버전 스코어러로 측정된 값 → 공정한 비교가 아님, 재측정 필요
- 다음: No RAG도 현재 스코어러로 재측정하는 공정 A/B 또는 RAG 힌트 품질 분석
- STATE.md 업데이트 완료 (Phase 07.1 Plan 4/4 진행 중, FAIL 원인 분석 대기)

### 18:14 — /clear 훅 에러 진단
- settings.json에 등록된 gsd-session-state.sh, gsd-phase-boundary.sh, gsd-validate-commit.sh 파일 3개가 실제로 존재하지 않아 SessionStart/PostToolUse/PreToolUse 이벤트에서 에러 발생
- session-start 훅도 using-superpowers/SKILL.md 미존재로 경고 메시지 출력 (graceful fallback)
- 해결책 제안: settings.json에서 없는 훅 항목 제거 또는 빈 stub 파일 생성

### 18:20 — GSD 업데이트 + Knowledge Compiler 재설치
- GSD 1.32.0 → 1.34.2 업데이트 완료, 누락 훅 파일 3개 자동 복구됨
- Knowledge Compiler --force 재설치: gsd-phase-researcher.md, gsd-verifier.md, new-project.md, new-milestone.md 패치 완료
- gsd-local-patches/ 에 4개 파일 백업됨 (gsd-phase-researcher.md, gsd-verifier.md, new-milestone.md, new-project.md) — /gsd-reapply-patches 병합 검토 필요

### 18:25 — Knowledge Compiler workflow 패치 수동 적용
- install.sh 버그: workflow 패치 코드 누락 (출력만 하고 삽입 없음)
- new-milestone.md, new-project.md에 .knowledge/ init 블록 직접 삽입 완료
- gsd-local-patches/ 백업 파일은 모두 knowledge-compiler 패치였음, 현재 파일로 완전 대체됨

### 18:27 — gsd-local-patches 삭제
- 모든 패치 적용 완료 후 gsd-local-patches/ 디렉토리 삭제

### 18:28 — ab_test_rag.py v3: full A/B 재실행 모드로 수정
- 기존 v2: A 조건(No RAG)이 DB 기존 score_final 그대로 사용(구버전 스코어러) → 공정성 문제
- v3: A/B 양쪽 모두 reset → translate → format → score 완전 재실행, 기존 results.json에서 배치 ID 재사용
- 변경 파일: `projects/esoteric-ebb/context/ab_test_rag.py` (커밋 e5c3ff3)

### 19:45 — score-worker sub-batch 수정 + dead code 전량 삭제
- score-worker: 20개 단일 LLM 호출 → 5개씩 sub-batch로 분리 (FormatWorker 패턴 적용), lease 만료 문제 해결
- dead code 8개 삭제: BuildScoreWarmup, BuildFormatWarmup, HasRichTags, StripTags, CountTags, BuildNamedVoiceSection, LoadDonePackItems, StripCodeFence
- ab_test_rag.py v3: 양쪽 조건 모두 재번역+현재 스코어러 측정, cwd=PROJECT_ROOT 버그 수정
- 변경 파일: worker.go, scorellm/prompt.go, tagformat/tags.go, tagformat/prompt.go, platform/checkpoint_store.go, shared/json.go, clustertranslate/voice_card.go

### 19:55 — 세션 재개: No RAG 스코어링 백그라운드 진행 중
- gsd-resume-work 실행: STATE.md 로드, DB 상태 확인
- go-v2-pipeline PID 94568 아직 실행 중 (score worker 백그라운드)
- DB 상태: done=119, working_score=20, pending_format=2, pending_translate=75 (216 items)
- ab_test_rag_results.json 스코어 아직 0 — 수집 전
- 다음: 스코어링 완료 대기 → No RAG 점수 수집 → With RAG 실행 → SUMMARY
